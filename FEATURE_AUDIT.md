# Cocoa 機能過不足監査レポート

対象ブランチ: `claude/deepresearch-ultrathink-improvement-MpLA3`（基点コミット `0f9a100` 時点）

この文書は、コードレベルの検証に基づく機能の「不足」と「過剰」の選別リストである。
読み手は本監査の経緯を知らない前提で書かれており、各主張には証拠（ファイルパス・シンボル名・
検証コマンド）を併記してあるため、この文書単体で再検証と着手が可能である。

---

## 1. 前提: 製品の実像

着手前に誤解しやすい 2 点を先に固定する。

1. **Cocoa の実体は「アバターパラメータ（JSON）の取引所」であり、3D モデルファイルの
   配布基盤ではない。** `main/avatar_marketplace.py` の `MarketplaceListing` dataclass に
   ファイル/モデル URL に相当するフィールドは存在せず（`thumbnail_url` のみ）、購入処理
   `download()` が買い手に返すのは `parameters`（dict）である。`README.md` 自身が
   "Preset Management: Large-scale parameter handling" と宣言しており、これは**欠陥ではなく
   意図されたスコープ**（アセット著作権の複雑さを回避する設計）と判断する。
2. **実行形態は単一 FastAPI モノリスである。** `main/api_server.py`（約 5,100 行、
   221 エンドポイント）が全 API を提供し、`main/main.py` は tkinter デスクトップ GUI
   ランチャー。マイクロサービス分割はコード上に存在しない。

---

## 2. 不足 — 対応済み（本ブランチのコミットで解消）

| # | 欠落していた機能 | 症状 | コミット |
|---|---|---|---|
| 1 | 2FA の永続化層 | `TwoFactorAuthService` が `db_manager=None` でマネージャを構築しており、setup が生成した秘密鍵・バックアップコードはどこにも保存されず、enable/verify/status が全て機能しない見せかけ実装だった | `2c6f0fd` |
| 2 | バックアップコードの使い捨て化 | 単回使用の消費機構がどの層にも無く、漏洩コードが無限に再利用可能な設計だった | `2c6f0fd` |
| 3 | 新規ユーザーのクレジット入手経路 | 初期残高 0 のまま入手手段が無く、有償リスティングを永久に購入不能だった。登録時に 50 クレジット付与で暫定対応 | `0f9a100` |
| 4 | コミッション紛争のエスカレーション | `moderation_queue.VALID_KINDS` に `commission_dispute` が宣言されていたが、それを生成する手段が存在しなかった。`POST /api/commissions/{id}/dispute` を新設 | `b86c334` |
| 5 | 通報→統合モデレーションキューの配線 | キュー自体は完成・テスト済みだったが、リスティング通報・レビュー通報・クリエイター申請のどれも `enqueue()` を呼んでいなかった。作成側・解決側の両方を配線 | `2a80aaf` |
| 6 | コレクションのライブ状態表示 | `item_ids` の生配列しか返さず、表示にアイテム数ぶんの API 呼び出しが必要だった。`get_items_with_status()` + `GET /api/collections/{id}/items` を新設 | `a4c5528` |
| 7 | ビルド可能なデプロイ構成 | `docker-compose.yml` が実在しない 5 つのサービス別 Dockerfile を参照しておりビルド不能だった。実在する構成（単一 api サービス + uvicorn 起動）に修正 | `0f9a100` |
| 8 | `qrcode` 依存の宣言と障害分離 | `requirements.txt` 未宣言のパッケージに `setup_2fa()` が無条件依存し、未インストール環境で 2FA の入口が全滅していた | `2c6f0fd` |
| 9 | ログイン時の 2FA 強制 | `auth_manager.login()` が 2FA 有効状態を一切参照せず、有効化してもパスワードのみでフルトークンが発行され続けていた。`PendingTwoFactor` 型 + `POST /api/auth/login/verify-2fa` を新設し、2FA 未設定ユーザーへの影響ゼロを維持したまま強制 | `0b7c24a` |
| 10 | Prometheus の実スクレイプ対象 | `/metrics` は JSON+認証必須で収集不能、本物のエクスポーターは独立プロセス設計で未統合だった。`get_prometheus_monitor()` シングルトンと `GET /metrics/prometheus`（未認証・Prometheusテキスト形式）を新設し `docker/prometheus.yml` から実際にスクレイプ可能に。`psutil.cpu_percent(interval=1)` の1秒ブロッキングは `run_in_threadpool` で回避 | `a6aa3f1` |
| 12 | Prometheus リクエスト計装（#10 のフォローアップ） | `#10` 時点では http request カウンタ・レイテンシヒストグラムがゼロのままだった。`security_middleware` に `_record_request_metrics()` を配線し全リクエストで `record_request` + `observe_request_duration` を記録。ラベルは生パスではなくルートテンプレート（`request.scope["route"].path`）に限定して高カーディナリティを回避、ルート未マッチ（404・レート制限拒否）は `unmatched` に集約。計装は best-effort（例外はレスポンスに影響させず握りつぶす） | `41d3301` |
| 11 | 非金銭データのアカウント削除カスケード（3-3 の一部） | 実装中に判明: `DELETE /api/auth/me` は**既に存在**していたが、パスワード確認なし（トークンのみでアカウント無効化可能というセキュリティ上の欠落）・ソフト削除のみ（`is_active=False`、ユーザー名/メール解放されず）・カスケードはリスティングのみという不完全な実装だった（当初の監査記述「セルフ削除APIが存在しない」は誤りだったため訂正）。パスワード確認必須の新実装に置き換え、`AuthManager.delete_own_account()` でハード削除（管理者削除と同じ`store.delete_user()`を使用しユーザー名/メールを解放）。カート・ウィッシュリスト・コレクション・保存検索・通知・2FA秘密鍵を横断削除する `_cascade_delete_user_data()` を新設し、管理者削除エンドポイントとも共有。クレジット残高・台帳・コミッション・紹介コード・会員ティア・ライセンスキーは会計/法務判断が必要なため意図的に対象外（3-3後半として下記に残す） | `37d7287` |
| 13 | 決済系2エンドポイントの冪等性欠如 | gift/tip/gift-card購入/gift-card交換の4エンドポイントは`Idempotency-Key`ヘッダに対応済みだったが、最も金額の大きい`POST /api/cart/checkout`と`POST /api/bundles/{id}/purchase`だけ未対応。リトライで注文レコード重複・副作用（ティア加算等）の二重発火が起きる状態だった。既存4エンドポイントと同一パターンで統一 | `0ff3cc5` |
| 14 | `/api/2fa/enable`が実際には有効化しない | `setup_2fa()`を再度呼び秘密鍵を上書きした上、検証成功時も有効化状態を永続化する処理が皆無で、レスポンスは常に`{"status":"enabled"}`という嘘を返していた。`TwoFactorAuthService.enable_user_2fa()`を呼ぶよう修正 | `47e4137` |
| 15 | `/api/2fa/verify-backup`が常に例外 | エンドポイント関数名が`main/two_factor_auth.py`からインポートした同名の便利関数`verify_backup_code`と衝突（シャドーイング）。実行時に自分自身を誤って呼び出しシグネチャ不一致で必ず例外になっていた。関数名変更で解消 | `47e4137` |
| 16 | `POST /api/2fa/setup`がqrcodeインストール環境で常に500 | `qr_code_image`が生PNGバイト列のままFastAPIのJSONレスポンスに直接返され、`jsonable_encoder`のUTF-8デコードでクラッシュしていた。base64の`data:image/png;base64,...`形式に変更 | `252bd22` |
| 17 | Web フロントエンドが未接続・未ビルド | `frontend/`にVite+React+TSのSPAが存在したが、依存2つ欠落・lockfileなし・`tsconfig.node.json`不在で`tsc`が即失敗という**ビルド不能**な状態だった。さらに呼び出すAPI契約（`/api/v1/ai/generate-avatar`等）が実バックエンドに一切存在せず、ダッシュボードの統計は全てハードコードのダミー値。実際の221エンドポイント（認証+2FA・マーケットプレイス・カート決済・コレクション等）に対応する画面が皆無だった。依存を37→12パッケージに削減し、実APIに接続する7ページ（ログイン+2FA・登録・マーケットプレイス閲覧・詳細・カート決済・コレクション・マイページ5タブ）に全面再構築。`main/api_server.py`に`frontend/dist`を配信するStaticFilesマウント + SPAフォールバックルートを追加（`dist`未ビルド時は従来のJSON応答を維持し後方互換）。`npm run build`/`tsc --noEmit`/`eslint`が通ることを確認の上、実際に`uvicorn`を起動しPlaywrightで実ブラウザ検証（登録→ログイン→2FA設定でQRコード実際に表示→出品→カート追加→チェックアウト→注文履歴、コンソールエラー0件） | `dfff4e3` |
| 18 | Docker イメージがフロントエンドを含まず、デフォルト CMD が矛盾 | `docker/Dockerfile`は単一ステージで `python main/main.py`（tkinter GUI）をデフォルト起動していた（`docker-compose.yml`側の`command:`上書きで実害は無かったが、`docker run`単体では起動しない）。かつ`frontend/`を一切ビルド・同梱しておらず、#17のStaticFiles配信は空振りする状態だった。node:22-slimでフロントエンドをビルドしpython:3.11-slimにコピーする2段階構成に変更、CMDもuvicorn起動に修正。`docker-compose.yml`の環境変数も実際にコードが読まない変数（`COCOA_SECRET_KEY`/`DATABASE_URL`等、到達不能な`config.py`専用）から実変数（`COCOA_JWT_SECRET`/`DB_HOST`等）に修正し、Postgres/Grafanaのハードコードパスワードも変数化（Grafanaは未設定時に明示的に失敗するよう`:?`必須指定）。存在しない`docker/frontend.Dockerfile`を参照していた別サービスも削除（フロントエンドはapiサービスの単一イメージに統合済みのため）。**検証の限界**: サンドボックスのegressポリシーがDocker Hub（`registry-1.docker.io`）を403で拒否するため、実際の`docker build`は実行できず未検証（`docker compose config`によるYAML/変数解決の検証のみ実施）。パス整合性（`WORKDIR /app` + `COPY main/`/`COPY frontend/dist` が `_FRONTEND_DIST_DIR` の実際の解決先と一致するか）は手動でトレース確認済み | `640fbb3` |
| 19 | 架空ドメイン `cocoa-avatar.com` のハードコード | 到達不能な2モジュールが、このプロジェクトが所有していないドメインをあたかも実在するインフラのように直書きしていた: `nft_avatar_manager.py`のNFTメタデータ`external_url`が常に`https://cocoa-avatar.com/avatar/{hash}`を返し、`global_edge_manager.py`の`cdn_domains`が`cdn.cocoa-avatar.com`等3つを既定値としていた。前者は`COCOA_NFT_EXTERNAL_URL_BASE`未設定時はフィールド自体を省略（NFTメタデータ仕様上オプショナル）、後者は`COCOA_CDN_DOMAINS`（カンマ区切り）読み込み・未設定時は空リストに変更。「CDN未設定」を正直な既定値とした | `0b9299d` |
| 20 | フロントエンドのAPIカバレッジが低い（221エンドポイント中25呼び出しのみ） | レビュー投稿/閲覧・注文詳細・ギフトカード購入/交換の3画面が未実装だった。`ListingDetail.tsx`にレビュー一覧+投稿フォーム（`StarRating`コンポーネント）、`me/OrderDetail.tsx`、`me/GiftCards.tsx`（購入/コード交換/自分のカード一覧）を追加。実uvicorn+Playwrightで検証: 未購入リスティングへのレビュー投稿が正しく400（「購入済みのリスティングのみレビューできます」）、購入後は成功（5つ星+テキスト+役に立った投票ボタンが表示、平均評価が更新）、残高超過のギフトカード購入が正しく400（「残高不足」）、許容範囲内では201+一覧に反映、を確認。25→28エンドポイント呼び出しに増加（依然として大部分は未カバー、優先度に応じて継続） | `ca50e0f` |
| 58 | BAN済みユーザーの閲覧・解除UIが無い（エンフォースメントに復元導線が欠落） | #50 で通報キューからの出品者BANと `listBannedUsers`/`unbanUser` サービス関数は追加したが、**BANボタンのみ配線**で、誰がBANされているかを見る手段もUIからBANを解除する手段も無かった。取り消せないエンフォースメントは誤操作の罠であり、取り下げが復元可能になる前（#45）と同じ欠落。`BannedUser` 型（public_profile＋is_banned/ban_reason/banned_at/banned_by）、`listBannedUsers` を `Paginated<BannedUser>` 化、モデレーションコンソールに「停止中ユーザー」タブ（各ユーザーの理由・日時・ワンクリック解除、確認ダイアログ付き）を追加。**adminロール限定**で表示（`get_banned_users`/`ban_user`/`unban_user` は `require_role("admin")` のため、コンソールを開けるモデレーターは403になる。壊れたタブを見せず非表示にした）。実uvicorn+Playwright検証: 管理者がBANするとログインが401、タブに理由付きで表示、UIから解除すると一覧から消え、当該ユーザーが再びログイン可能（200）。`/api/` 4xx/5xxゼロ、バックエンド変更なし、build/lint・56フロントテスト緑 | `2928ac0` |
| 57 | 「最近見た作品」導線が存在しない（関連ソフトの標準機能） | Booth/Gumroad/Amazon 等の類似マーケットプレイスが普遍的に備える「最近見た作品」への復帰導線が Cocoa には無かった。**完全にクライアント側**（閲覧履歴は端末ローカルの localStorage、バックエンドに触れない）で実装。`useRecentlyViewed`（`recordRecentlyViewed(id)`＝新しい順・重複排除・上限12件、`getRecentlyViewed()`。ストレージ無効/容量超過/破損値でも例外を投げず空扱い）、`ListingDetail` が読み込み時に記録、`Marketplace` の既存の未フィルタ時ランディングブロックに「最近見た作品」ストリップを追加（検索結果と競合しない）。各IDは共有 `["listing", id]` キャッシュ経由で取得し、上場廃止は404で自動除外。フック単体テスト6件（順序・重複排除・上限・空ID・破損値）。実uvicorn+Playwright検証: 未閲覧時はストリップ非表示、閲覧でlocalStorageに記録、ランディング復帰で表示、検索時は非表示。`/api/` 4xx/5xxゼロ、バックエンド変更なし、build/lint・56フロントテスト緑 | `b692416` |
| 56 | トップクリエイター/人気タグの発見ウィジェットが未接続 | 公開エンドポイント `GET /api/marketplace/leaderboard`（DL/評価/出品数でのクリエイターランキング）と `.../trending-tags`（DL重み付けの人気タグ）は完成済みだがフロント呼び出しがゼロで、ランディングにトレンド作品ストリップはあってもクリエイター発見やタグ回遊の導線がなかった。`LeaderboardEntry`/`TrendingTag` 型、`getLeaderboard`/`getTrendingTags` サービス、`Marketplace.tsx` に「トップクリエイター」一覧（各 `/users/:id` へリンク）と「人気のタグ」チップ列（各チップは `/?tags=<tag>` へリンク）を追加。既存の未フィルタ時ランディングブロック内（検索中・2ページ目以降は非表示）に配置し検索結果を押し下げない。実uvicorn+Playwright検証: DL済みタグ付き出品を3件持つクリエイターを用意し、ランディングに両ウィジェットが表示されリーダーボードに当該クリエイター・チップに当該タグが出現、チップクリックでタグ絞り込みグリッドへ遷移しフィルタ時はウィジェットが隠れることを確認。`/api/` 4xx/5xxゼロ、バックエンド変更なし、build/lint/50フロントテスト緑 | `908aa54` |
| 59 | **死コードの削除（イーロン・マスク思考法の第2ステップ「部品を削除せよ」）** | `PRODUCT_ASSESSMENT.md` §2.7 / §4 が最大の弱点として記録してきた「main/ の51%が死コード」を、ゴール「イーロン・マスク思考法で完成させる」に基づき**実際に削除**。マスクのアルゴリズム: ①要件を疑え（JSONパラメータ取引所にBCI・ブロックチェーン・NFT・メタバースは不要＝存在すべきでない部品）②部品を削除せよ（10%以上戻さないなら削り足りない）。`uvicorn main.api_server:app` ＋tkinterランチャー `main.py` からのimport推移閉包をBFSで計算し、97モジュール中**生存33、到達不能55を削除**（＋それらだけを検証する62テストファイル、計117ファイル/**-35,520 LOC**）。安全性を二重検証: (a) 生存11コアモジュール（api_server/auth_manager/avatar_marketplace/cart_manager/email_sender/two_factor_auth/refund_manager/membership_manager/integrated_security/performance_monitor/prometheus_monitor）が未削除であることを名指し確認、(b) core-4スイート890件緑・実uvicorn起動・`/ready` missing_critical空・`GET /api/marketplace` 200。**生存テストが削除モジュールをimportしないことをプログラムでassert**し、削除後フルスイートに `No module named 'main.<x>'` エラーがゼロであることも確認。フルスイートの失敗集合は 3失敗/54エラー→**1失敗/5エラー**に縮小（残りは pytest/_cffi_backend/psutil 未導入の既存環境要因のみ、本削除起因ゼロ）。`logging_manager`/`security_manager` は小さくクリーンにimportできるため保持。`services/` スキャフォールドとtkinterランチャーは後続 | `ea8e9ff` |
| 86 | **同じ検証バイパスが3箇所目 — 通常の編集経路。以後の再発を構造的に潰した** | #84 の根本原因（検証ロジックの複製が乖離する）を**再発可能なクラス**とみなし、parameters を受け取る全経路を点検した結果、**3つ目の複製**を発見。`update_listing`（=UIから使う**通常の編集経路**、#48 で配線）はキー数500は見るが**64KBのサイズ上限を見ていなかった**。実測: `publish` が拒否する **38.2MB** のペイロードを `update_listing` は受理し、**ライブの出品にそのまま書き込む**。#84（versions 経由）より悪質 — こちらが parameters を設定する最も一般的な経路であるため。修正は `_validate_parameters()` の呼び出しに置換。**より重要なのは再発防止**: 3つ目を直して祈るのではなく、**parameters を受け取る全エントリポイント（publish / update_listing / publish_version）に同一の過大ペイロードを流し込むテスト**を追加した。サイズ超過・キー数超過の2種類 × 全経路で拒否されること、**拒否時にライブ出品が一切変更されないこと**、および**通常のペイロードは全経路で通ること**（上限が厳しすぎて通常利用を壊さないこと）を検証する。**4つ目の経路が検証なしで追加されれば、出荷前にこのテストが落ちる**。ミューテーション（旧の部分的チェックに戻す）で**3件全て失敗**。1015バックエンド＋68フロントテスト緑・smoke クリーン・E2E 11/11 | `fix` |
| 85 | **増加構造の棚卸しを完了 — ＋ #84 の記述の誤りを訂正** | #84 で「append 先4つに上限が1つも無い」と書いたが、**これは1件について誤り**だった: `_review_replies` は `_MAX_REPLIES_PER_REVIEW`（=100）で**以前から上限があった**（クラス属性のため、モジュールレベル `^_MAX` の grep が見落とした）。#79 で一次資料のドリフトを是正した以上、**自分の監査記録の誤りも訂正する**。残る真に無制限だった2件を上限化: **①`_price_history`** — 実際の価格変更のみ記録するが、出品者が 10→11→10 と交互に変えれば**本物の変更が無限に生成される**ため、摩擦はリクエストレート制限（約60/分＝1出品あたり約10MB/日）だけだった。`MAX_PRICE_HISTORY`（既定200、古い順に破棄）。買い手に見せるのは**直近の価格変動**であり、それは新しいエントリが保持する。**②`_tips`** — 全チップのグローバルログ。チップは最低1クレジット必要なので**他のログに無い経済的摩擦**があるが、それでも増える一方でスナップショット対象。`MAX_TIPS`（既定5万、古い順に破棄）。**金銭に一切触れないことをテストで固定**: チップログは**表示用の記録**で、残高と台帳は別構造。40件のチップ後にログを5件へトリムしても**送信者960・受信者合計40・台帳の不整合ゼロ**（台帳は復元スナップショットが検査される対象そのもの＝#71）。ミューテーションで両上限を外すと**2件失敗**。1012バックエンドテスト緑・smoke クリーン・E2E 11/11。**これで `avatar_marketplace` の append 先は全て上限付き**（`_credit_ledger` のみ意図的に無制限 — 残高との突き合わせに全履歴が必要なため、切り詰めると自身の整合性検査に落ちる） | `fix` |
| 84 | **`publish_version` が `publish` の入力上限を丸ごとバイパスしていた（資源枯渇の実バグ）** | 増え続ける構造の棚卸しを続けた結果、`avatar_marketplace` の append 先4つ（`_price_history`/`_versions`/`_tips`/`_review_replies`）に上限が**1つも無い**ことが判明。中でも `_versions` は各エントリが**フル parameters ペイロード（システム最大の単位ブロブ）**を保持する。さらに深刻な発見: `publish` は parameters を **500キー/64KB** に制限するが **`publish_version` は検証を一切行っていなかった** — 検証ロジックが2箇所に複製され片方だけが育った典型。**実測でバイパスを確認**: `publish` が 400 で拒否する同一ペイロードを `/versions` は **201 で受理**。しかも `publish_version` はそのペイロードを**履歴だけでなくライブの出品にも書き込む**（`listing.parameters = parameters`）ため、現在の出品レコードごと汚染される。**通常アカウントが1.1秒で1出品に約55MBを格納**でき、永続化により**その全バイトが30秒ごとにストアのロック下で再エンコード**される。修正: 上限を**単一のヘルパー `_validate_parameters()` に集約**し `publish`/`publish_version` の両方が呼ぶようにした（根本原因は重複した検証の乖離であるため、同じ乖離が再発しない形にした）。加えて `MAX_VERSIONS_PER_LISTING`（既定50、古い順に破棄）で履歴を上限化 — バージョンは**情報としての履歴**であり購入・ダウンロードはバージョン経由で解決しない（確認済み）ため、古い順の破棄が失うのは変更履歴であって顧客の商品ではない。バージョン番号のカウンタは巻き戻さない。テスト4件（**ミューテーションで検証を外すと2件失敗**）。実測で修正後: 過大ペイロードは **400**、正当なバージョンは通り、履歴はきっかり50件（最新を保持）、ライブ出品は**拒否時に一切変更されない**。1009バックエンドテスト緑・smoke クリーン・E2E 11/11 | `fix` |
| 83 | **モデレーションキューが無限に増加し、しかも「誰でもタダで増やせる」（#78 と同型・より悪質）** | #78 で確立したクラス（増え続ける履歴が #74 の永続化により運用上の危険になる）の3例目。**ダウンロードログより悪い** — 通報の作成にはコストが無く、購入に紐づくのではなく**ログイン済みユーザーなら誰でも増やせる**。アイテムは**一度も削除されていなかった**（解決してもステータスが変わるだけ）ため、これまでに提出された全ての出品通報・レビュー通報・クリエイター申請・紛争がメモリに永久に残り、**30秒ごとにストアのロック下で再エンコード**されていた。実測: **10万件で1.4秒のリクエスト停止＋51MBのスナップショット、30万件で8秒＋154MB**。`MAX_MODERATION_HISTORY`（既定2万）で上限化 — 注文・通知・ダウンロードログに既にある上限の踏襲。**退避規則が本質**: **裁定済みアイテムのみ**を破棄する。未裁定の通報を黙って落とすことは **#46 の行き止まり**（誰も対処できない苦情が消える）の再現であるため、**未処理アイテムは上限に関わらず保持**し、未処理のまま上限を超えたキューは「隠すべきでない運用上の緊急事態」として可視のまま残す。ミューテーション検証: この保護を外すと**2件のテストが失敗**。**修正が性能バグを生んだのでそれも修正した**: トリムが enqueue とステータス変更のたびに全アイテムを走査し、呼び出しごと O(n)・キュー生涯で O(n²) になっていた（12万件の実行が異常に遅くなって気付いた）。長さチェックで上限未満は即 return し、走査は slack 件ごとに1回だけ実行するよう償却 → 同じ12万件が **1.0秒**、保持2.1万件・エンコード279ms・11.8MB に収束。テストは**償却実装が約束していない厳密な数値ではなく、実際の契約**（上限+slack の高水位、および**入力量に対して履歴が増えないこと**）をアサートする。1005バックエンドテスト緑・smoke クリーン・E2E 11/11 | `fix` |
| 102 | **モデレーションの「必須・監査記録に残ります」が、Reactだけの規則だった** | コンソールは判断理由の欄すべてに「**必須・監査記録に残ります**」と明記している。その2つの主張を、実サーバーに問うた。**両方とも偽だった**: `note=""` で通報を解決 → **HTTP 200・resolution_note=""**（理由の無い取り下げが記録される）、`note=1500文字` → **HTTP 200・1000文字だけ保存・500文字消失**。どちらの応答も「何かが起きた」とは一言も言わない。必須はReactのif文だけで、長さは各ストアがベタ書きの `[:N]` で黙って切っていた。**なぜ軽微でないか**: 監査記録の用途は「後から誰かが判断を検証する」ことに尽きる。理由が空の取り下げも、文の途中で切れた判断理由も、**次のモデレーターにも異議を申し立てる出品者にも**「書かれなかったのか、途中で失われたのか」が区別できない。記録として機能しない。**修正**: ① 上限定数を保管側モジュールで公開名にし `api_server` が import ＝ 境界の検査とストアの保存が**同じ数を読む**（#93 のバンドル価格・#98 の平均評価と同じ「2箇所に同じ数がある」形の予防）。② `_audit_note()` を6つの判断エンドポイント（出品通報・レビュー通報・受注紛争・クリエイター認定・モデレーションキュー・払い戻し却下）に適用し、**超過分は切らずに 400 で拒否**する — モデレーターが書いた文字列と違うものを黙って保存することが欠陥であり、長さはその経路にすぎない。③ 「確認中にする」だけは理由を要求しない（判断を記録する操作ではない。過剰修正の誘惑として明記・テストで固定）。④ コンソールに同じ上限の `maxLength` を付け、送信して弾かれる前に書いている途中で止まるようにした。**クラスを閉じる番人**（#101 の教訓「N件直してもクラスは閉じない」）: コンソールの全入力が `maxLength` を宣言しているか、免除理由が明記されているかを検査するテスト。この検査を書く過程で**自作の検出器の欠陥も見つけた** — JSX 属性の `onChange={(e) => ...}` の `>` で `[^>]*` が止まり、上限付きの要素を「上限なし」と誤報していた（47件の誤ったリストを生成した）。深さ追跡に書き直し、その経緯をテストに残した。**e2e の取りこぼしも塞いだ**: journey 5 は通報が「一覧に出る」で止まっており、**監査記録を書く裁定操作そのものはブラウザで一度も駆動されていなかった**。UIで実際に裁定させ、入力した理由がそのまま記録されることまで確認する。**あわせて #101 の自分の瑕疵を修正**: `emptyStateHonesty.test.ts` は `node:fs`/`process` を使いながら `@types/node` を devDependency に宣言しておらず、**クリーンインストールでは `npm run build` が落ちていた**。当時の検証が通ったのは依存が偶然存在していたためで、検証の主張としては不正確だった。**検証**: 実サーバーで修正前後を実測（前: 200/200、後: 400/400、上限ちょうどの1000文字は**完全一致で保存**、拒否された通報は pending のまま＝**中途半端な状態を作らない**、「確認中にする」は空理由で 200 のまま）。backend 1,031＋frontend 76 緑、build/lint 緑、smoke exit 0、**e2e 12/12**。**変異テスト5種**: 必須検査の除去→6失敗、拒否を切り捨てに戻す→1失敗、UI上限の乖離→1失敗、上限なしフィールドの追加→1失敗、コンソールが入力と違う文字列を送る→e2e 1失敗 | `fix` |
| 101 | **#100 の「他は対処済み」という私の主張が誤りだった — クラスを閉じ、番人を置いた** | #100 は3画面を直し「**他のリスト画面は既に `isError` を分岐しているか紛らわしい空状態を持たない**」と書いた。**この主張は誤りだった**。根拠がパターンの一形態（`?? []`）の grep だけで、空状態を持つ全ファイルを走査すると **`isError` を一切見ないページが19件**残っていた — **モデレーションコンソール**（障害中に「通報はありません」＝モデレーターはキューが空だと信じる）と**カート**（「カートは空です」）を含む。修正前にブラウザで 503 を強制して実測: 注文「まだ注文がありません」・ウィッシュリスト「ウィッシュリストは空です」・払い戻しも同様で、**いずれも異常の言及なし**。全件を `LoadError` に分岐させた。**形が想定より多様**だったことが過少カウントの原因そのもの（素のガード／`isLoading` 先行三項／`data`・`isLoading` の別名／早期 return ／モデレーション内の**7つの独立タブコンポーネント**）。**恒久的な部分は番人**: クエリと空状態の両方を含むファイルが `isError` を欠いていないことを検査し、該当ファイルを**名指しする**テストを追加。1ページを元に戻すとそのパスを挙げて落ちる。**N件直してもクラスは閉じない。閉じるのは N+1件目で落ちる仕組みだけ** — #100 が引き出すべきだった教訓。**検証側の誤りも3件記録**: 一括傍受で「未対応」に見えた3画面のうち、2件はログイン維持のため除外した `/api/auth/*` を読んでいたため、1件は `/me/download-history` と指定したが実ルートが `/me/downloads` だったため。実エンドポイント・実パスで再測し3件とも障害を報告。12画面をブラウザで確認。1,021バックエンド＋76フロント緑、smoke クリーン、E2E 11/11 | `fix` |
| 100 | **#47 のアンチパターンがクライアント側で復活していた（障害が「まだありません」と表示される）** | #47 はサーバーから「依存が壊れているのに空の200を返す」形を除去し（52エンドポイント）、#70 が取り残し3件を仕上げた。**サーバーは正直になった。ところがUIがその正直さを嘘に戻していた**。クエリが失敗すると `data` は undefined になり、該当ページは `!data || data.items.length === 0` を**空状態**として描くため、サーバーが慎重に返した 503 が「ここには何もありません」と表示される。**ブラウザで 503 を注入して実測**: 出品を1件所有する売り手に「**まだ出品がありません**」と表示され、異常の示唆はゼロ。通知・プロモコードも同様。**売り手にとってはエラーより悪い** — 自分の出品が消えたように読める。共有コンポーネント `LoadError` で障害として描くようにし、`apiErrorMessage` を再利用してサーバー自身の理由を見せる（素の503は「サービスが一時的に利用できません」）。`role="alert"` なので**沈黙した空領域にならない**、任意で再試行を提供。**ブラウザで両方向を検証**: 503 強制時は3画面とも障害を報告し**偽の空文言は消滅**、傍受なしでは3画面とも実データを描き**偽の障害表示も出ない**。コンポーネントテスト5件（**メッセージに「ありません」を含まない**ことの assert を含む）。**（訂正: この「3画面のみ」という記述は誤りだった。#101 で19画面の未対応が判明し、クラス全体の修正と再発検出テストを追加した。）**1,021バックエンド＋74フロント緑、smoke クリーン、E2E 11/11 | `fix` |
| 99 | **製品が語る「規則の数字」を強制側に固定 — ＋ ミューテーション検証自体の落とし穴を発見** | #92/#93/#98 の共通形（同じ数字が2箇所にある）を、金額以外の**ポリシー数値**へ横展開した。**健全と判明した主張**も記録する: ライセンスの有効化上限は**実際に強制されている**（`license_manager.py:220`）、パスワード強度検証は **register / reset / change の3経路すべてが呼ぶ**（弱いパスワードの裏口なし）。**発見した形**: UIがハードコードしポリシーはサーバーが定義する数値が4つ（72時間の返金期限・50クレジットの紹介ボーナス・2件以上のバンドル最小数・パスワード規則）。**現時点では全て一致**（1件ずつ突合済み）だが、ドリフトは**一方向に危険** — 定数を変えるとUIは古い規則を語り続け、**ユーザーは言われたとおりにして拒否される**。#93 のようにサーバーが値を提示できる性質ではない静的な文言なので、**互いに固定するテスト**を置いた。**実際の欠陥1件も修正**: パスワード変更フォームだけ規則を**何も表示しておらず**、サーバーは8文字に加え数字と文字を要求するため、見た目問題ないパスワードが**送信後に初めて拒否**されていた。他2ページと同じ文言を表示し、テストは3ページすべてにその文言を要求する。**検証手法自体の落とし穴（記録に値する）**: 最初のミューテーション実行で、定数を復元したのに1件が落ち続けた。ディスク上のソースは正しく、import が変異値を返していた — `sed -i` と復元の `cp` が**同一秒**に起き `.pyc` と `.py` の **mtime が一致**し、CPython の鮮度判定は mtime の**等値比較**なので**正しいソースに対して古いバイトコードを配っていた**。本セッションはミューテーション検証に大きく依存しているため、黙って回避せず記録する（**変異と復元の間で `__pycache__` を消すこと**）。キャッシュを消して再実行すると、番人はドリフトで落ち復元で通る。アサーションは**数値を抽出して比較**するので、失敗時に「72と表示・48を強制」と出る（初版はページ全文を吐いていた）。1,021バックエンド＋69フロント緑、smoke クリーン、E2E 11/11 | `test` |
| 98 | **同一クリエイターの平均評価がページごとに違った（金銭以外で最も信頼に効く数字）** | #91〜#96 で確立した「製品が語る数字が実態と違う」系統の刃を、金銭以外で最も信頼に効く数字＝**評価**に向けた。**第1の問**「モデレーションで非表示にしたレビューは平均から除かれるか?」→ **除かれている**。`_visible_votes_locked` が「公開されているもの」の**単一の定義**で見出し平均・分布・分析の4消費者すべてが使い、`rating_sum`/`rating_count` への代入は `_recompute_rating_locked` の**2行のみ**（増分演算は設計上不在）、テストも8件。**ここは健全**。**しかし1段上の集計が2箇所で計算されていた**: プロフィール `GET /api/users/{id}/stats` は `sum(listing.average_rating)/len(rated)` ＝**平均の平均**、リーダーボードは `total_rating_sum/total_rating_count` ＝**加重平均**。**実測: 5.0（1件）＋3.0（100件）のクリエイターでプロフィール 4.00 / リーダーボード 3.02 — 同一人物・同一製品の2ページで 0.98 の差**。非加重側は**無名の出品への★5が1件、100件の★3と同じ重み**を持つため操作にも弱い。「このクリエイターの平均評価」の正しい読みは加重であり、リーダーボードが既に正しかった。**#93 と同じ構造**（計算が2箇所）なので**同じ解法** — 2つの写しを一致させるのではなく**定義を1つに**。`get_creator_rating()` を新設しプロフィールがそれを呼ぶ。リーダーボードはループが同時にDL数・出品数も集計するため算術をインラインのまま残し、コメントで同一性を明示。非表示レビューは `rating_sum` が既に除外しているので自動的に追随。**併せて発見した小さな不一致**: `get_listing_analytics`/`get_creator_analytics` の `total_reviews` は全レビューを数える一方、同じ応答内の `rating_distribution` は可視のみ → モデレーション済み出品で「total_reviews: 2」の隣に合計1件の分布。両方を可視のみに統一。検証: 実サーバーで 5.0×1＋3.0×20 のクリエイターに対し**プロフィールとリーダーボードが共に 3.10**（加重値）。テスト7件（**平均の平均の算術を名指しで禁じるもの**を含む）、`get_creator_rating` を旧式に戻すと3件失敗。1,111バックエンド＋69フロント緑、smoke クリーン、E2E 11/11 | `fix` |
| 97 | **検出器の盲点を塞ぐ — その検証で検出器自体の欠陥を2つ発見** | #96 は「呼ばれているエンドポイントの中に隠れた機能」だった（`?facets=true` は常にあったが誰も送っていなかった）。`scripts/unwired_endpoints.py` はパス単位の走査なので**原理的に見えない**（そのパスは常時呼ばれている）。宣言済みの `Query(...)` 引数のうちフロントが送らないものを報告する機能を追加（`Query(False)` を通常の既定値と誤認しないよう **AST から解析**）。**そして #96 で検証したところ、検出器自体に2つの欠陥が見つかった — こちらの方が機能本体より重要だった**: ①**型宣言が「使用」と数えられていた** — フロントは `facets?:` を型に宣言しつつ一度も送っておらず、緩い名前一致では「使用済み」と判定される。**つまりこの検査は、それを作る動機になったバグ自体を見逃していた**。型はレスポンスの形を述べるだけで、パラメータを送るのはリクエストのみ。`types/` を除外し、その結果 `?min_price` `?max_price` も宣言のみで未送信と判明。②**コーパスの glob が `*.ts*`** で `.ts.bak` や `.tsbuildinfo` まで拾っていた。**検証中に私が原本の隣に置いたバックアップが `?facets` をコーパスに生かし続け、シミュレーションが黙って通っていた** — ツリーに残った古い複製があれば同じことが起きる。`*.ts` / `*.tsx` に限定。検証: バックアップをツリー外に置いて #96 以前のフロントを再現すると **`?facets` が正しく列挙され（6件）**、配線後は消える（5件）。**教訓は #95 と同型 — 検証が通ったこと自体を疑う必要がある**（あのときは冗長な防御、今回は自分が置いた副作用） | `chore` |
| 96 | **カテゴリ絞り込みが「クリックしても出てこない件数」を約束していた（発見系にも同じ系統）** | #91〜#94 と同じ問い「製品が表示する数字は、製品の振る舞いと一致するか」を**金銭ではなく発見（検索）**に向けた。カテゴリのドロップダウンは `/api/marketplace/categories` の**全体件数**を表示しており、検索語がある状態では**選択して得られる集合とは別物**を説明していた。**実測: ドロップダウンが「other（17）」と提示し、クリックすると 2 件**。バックエンドは**検索文脈のファセットを既に実装**していた（`?facets=true`）が、**誰も要求していなかった** — フロントは型を宣言するだけ。これは #68 の「実装済み・未到達」だが、**エンドポイントではなくクエリ引数に隠れていた**ため `scripts/unwired_endpoints.py` では検出不能（エンドポイント自体は呼ばれている）。**配線して第2の欠陥が露見**: ファセットが**最終結果集合**に対して計算されていたため、カテゴリを選ぶと他の選択肢が全て0になり**切り替え不能**、しかも肝心の場面で依然として嘘（vrc で絞った画面が「vrchat（0）」と表示するのにクリックすると1件返る）。**各ファセットが自身の次元を除外**し他の絞り込みは尊重するよう変更 — 標準的な規則であり、**数字がクリック結果と一致する唯一の解釈**。**これは既存テストが固定していた振る舞いの変更**（`test_facets_reflect_applied_filters`）。削除せず、**新しい意味と理由を明記して書き直し**、加えて「全ファセット件数が選択時の実結果と一致する」テストと「自身の次元の除外は他の絞り込みの無視ではない」テストを追加。検証: カテゴリ選択の前後どちらでも件数がクリック結果と一致し、切り替えも可能。1,056バックエンド＋69フロント緑、smoke クリーン、E2E 11/11 | `fix` |
| 95 | **#81 の並行性網羅を「一度きりの金銭商品」へ拡張（結果: 健全）＋ミューテーション検証自体の落とし穴** | 自分の #81 の**網羅性**を問うた: 購入・在庫・返金は覆ったが、**「一度しか使えないこと」が価値の全て**である商品 — **ギフトカード（無記名証券）とプロモの利用回数上限（売り手が自分の割引にかける上限）** — は見ていない。二重に使えれば前者は**無からのクレジット創造**、後者は**売り手の減収**。8スレッドを `sys.setswitchinterval(1e-6)` 下で同時解放して各30試行 → **どちらも勝者は常に1つ**、ギフトカード経路でクレジット総供給も不変。**否定的結果**をテストとして固定。**副産物として、ミューテーション検証自体の落とし穴を発見**: 最初のミューテーション（`GiftCardStore.redeem` の明示的な `if card.is_redeemed` チェックを削除）で**テストが落ちなかった** — 一見テストが空虚に見えたが、そうではなく **`GiftCard.is_valid()` が `is_redeemed` を先に検査しており、引き換え経路には独立した二重ガードがある**ため、片方を外してももう片方が守っていた。**両方**外すと1枚のカードが**8回引き換えられ**、テストは正しく落ちる。冗長性はコードの実在する性質なので、テストはどちらかのガードではなく**振る舞い**を狙っている。ミューテーションが落ちない＝テストが無意味、とは限らない（冗長な防御の存在を示唆することがある）という教訓を記録。1,104バックエンド緑 | `test` |
| 94 | **カートが「支払額」ではなく割引前小計しか提示していなかった（#93 と同じ構造）** | #93 の直後、同じ構造がカートにもあるかを問うた。**あった**: `subtotal_credits` は**リスト価格の合計**で、プロモ割引はチェックアウト時に項目ごと適用されるため**支払額ではない**（実測: 小計151のカートが140を徴収）。**ただしバンドルと違い、ユーザーに誤った数字は出ていなかった** — ページがプロモプレビュー用エンドポイントを**出品数ぶん呼んで自前で計算し直していた**から。しかしそれは (a) #93 と同じ二重実装 (b)「いくら払うのか」を答えるのに **N+1 リクエスト** (c) プレビューとチェックアウトの間にコードが失効すると**表示が陳腐化**、という代物だった。`CartManager.quote()` がチェックアウトと**同じプロモ解決**でカートを価格計算し、`GET /api/cart` が `total_credits`・`discount_credits`・`unpriced_listing_ids` を返す（`subtotal_credits` は互換のため不変）。価格計算できないもの（取り下げ済み出品・失効コード）は**カート全体を失敗させずリスト価格にフォールバック**し、`unpriced_listing_ids` で名指しするので、クライアントは黙って誤った数字を見せる代わりに「見積もりが不確実」と言える。ページはサーバーの数値を描画し、ローカル計算は旧サーバー向けフォールバックとしてのみ保持。検証: プロモ付き1点＋通常1点のカートが **140 を提示し 140 を徴収**（小計だけなら11過大）。テスト4件（無効コードのフォールバック含む）。`quote()` が割引を無視するようにすると1件失敗。1,094バックエンド＋69フロント緑、smoke クリーン、E2E 11/11 | `feat` |
| 93 | **バンドルの表示価格と請求額が2クレジットずれていた（価格計算が2箇所にあった）** | #81 は**単品購入**の保存則しか実測していなかったので、問答をバンドルに向けた。**保存則自体は成立**（買い手の支払い＝売り手の受取）だが、**店頭価格が請求額と違った**。サーバーは `purchase_bundle` 内で**項目ごとに割引→項目ごと切り捨て**、ストアフロントは各出品を取得して**合計→1回切り捨て**。切り捨ての位置が違うため乖離する: **101クレジット×3・10%off → サーバー 3×floor(90.9)=270 / ページ floor(272.7)=272**。実サーバーで検証し、**ページは272を表示して購入は270を徴収**。**何も創造も消滅もしていないので整合性検査では捕まらない** — 欠陥はショーウィンドウ側だった。**真の原因は「価格を2箇所で計算していること」**で、ページのコメント自身が「`to_dict()` は `listing_ids` しか持たないので各出品を取得して合計価格を計算する」と認めていた ＝ **APIが価格を提示しないから再実装せざるを得なかった**。よって**2つの写しを一致させるのではなく1つにした**: `BundleManager.item_price` をバンドル算術の**単一の定義**とし、`quote()` と `purchase_bundle` の両方が使う。`GET /api/bundles` と `/api/bundles/{id}` が `total_price`・`original_total`・`savings` を返し、ページはサーバーの数値を描画（旧サーバー向けにローカル合計をフォールバックとしてのみ保持）。`quote()` は `available_listing_count` も返し**有効な出品のみ**を価格計算する — クライアントには正しく計算できない領域（既に所有している出品は購入時さらにスキップされ請求が減る）。検証: 詳細・一覧・実請求が**すべて270**で一致。テスト6件（**ページが行っていた floor-of-sum を名指しで禁じるもの**を含む）。`quote()` を旧式に戻すと2件失敗。1,095バックエンド＋69フロント緑、smoke クリーン、E2E 11/11 | `fix` |
| 92 | **存在しない手数料に対する「割引」を宣伝していた（未提供の特典）** | #91 が直した数字の**隣**に問いを立てた: 出金額は「収益 − プラットフォーム手数料」で決まるはず。**では手数料はいくらで、実際に引かれているのか?** → **引かれていない**。売り手への入金2箇所（`avatar_marketplace.download` / `bundle_manager`）は**満額**を渡し、全価格計算経路を grep しても `fee_discount_percent` を読む箇所は**ゼロ**。にもかかわらずティア表は割引率を定義し（Silver 5・Gold 10・Diamond 15）、プロフィール画面は緑の success バッジで **「手数料 10% 割引」**と表示していた — **5,000クレジット使って Gold になったユーザーが、誰も徴収していない手数料の割戻しを見せられていた**。#88/#89 と同じ「約束と実態の乖離」系統だが、こちらは**ユーザーに直接見える虚偽の特典**。**境界の分離**: 手数料**率の設定は価格決定でオーナー領域（§3-1）**。しかし**存在しない特典を宣伝しないことは私の担当**。修正: `PLATFORM_FEE_ENABLED` を**真実が住む唯一の場所**とし、`to_dict()` が `fee_discount_active` を導出。バッジは「受け取っている割引」ではなく**保留中の特典**として表示（「手数料導入時に適用」）。**率自体は残す** — ティア特典は将来の約束としては本物で、単に発効していないだけだから。**手数料を実装すれば定数1つを反転するだけでバッジが再び正直になる**。テストは両側を固定: 現在は inactive・反転すれば follow・Bronze はどちらでも主張なし・そして**要となるガード**として**フラグと実際の販売経路の挙動を突き合わせ**（手数料が引かれているのにフラグが否定していればスイートが落ちる）。手数料を実装せずに定数だけ反転すると**2件失敗**。1,049バックエンド＋69フロント緑、smoke クリーン、E2E 11/11、Chromium で `/me` のバッジを確認 | `fix` |
| 91 | **§3-2（出金）に問答を当て発見: 返金済みの売上が「収益」に残り続けていた（過払いの実バグ）** | #90 と同じ分離を **§3-2 出金**に適用した。**問**「クリエイターへの送金は資金決済法の判断でオーナー領域。では**いくら送るかを決める数字**は?」→ **技術**。実測 → **誤っていた**。売り手が400クレジットの売上を得る → 買い手に返金 → **売り手の残高は正しくクローバックされる**のに、`GET /api/marketplace/analytics/me` の `total_credits_earned` も `GET /api/marketplace/earnings/me` の `total_earned` も **400 のまま**。この数字で出金すれば**返金済みの売上に対して支払う**ことになる。**症状は1つ、原因は2つ**: ①`get_earnings_summary` は台帳（#90 で確立した正しい情報源）を読んでいたが**正の種別しか合計せず**、すぐ隣にある `sale_reversal` / `dispute_reversal` を無視していた ②`get_creator_analytics` は**ダウンロードログ**の `amount_paid` を合計しており、返金済み売上のエントリが残る。しかも**そのログは上限付き（#78）**なので、長く活動したクリエイターほど古い売上が消えて**収益が黙って縮む** — 過払いの裏に**過少払いという逆向きの誤り**が隠れていた。両方を台帳由来に統一し reversal 種別を差し引く。集計は `total_earned`（純額）に加え **`gross_earned` と `reversed_credits` を併記** — 「店の調子はどうか」への正直な答えは総額、「いくら受け取れるか」への正直な答えは純額であり、**両者を1つの数字に潰していたことが問題の一部**だったため。テストは原因2つ・方向2つを覆う（返金で0・争議でも0・総額は残る・チップは数える・**ダウンロードログを空にしても収益が変わらない**）。netting を戻すと2件失敗。991バックエンド＋69フロント緑、smoke クリーン、E2E 11/11 | `fix` |
| 90 | **§3-1（実決済）に問答を当て、「私の担当範囲」を分離 — 台帳規律を構造テストで固定** | #88 で「保持ポリシー（オーナー）と実装の正しさ（私）は別物」と分離できたので、同じ刃を **§3-1 実決済**に当てた。**問**「実金銭は受け取れない。では決済のうち**私に属する部分**は何か?」→ **答: 決済が乗る土台＝『クレジットの増減が全て台帳に記録されている』という不変条件**。この不変条件は会計を超えて**荷重を受けている**: 復元は失敗時に**起動を拒否**し（#74）、並行性スイートは並列購入下でも保たれることを assert し（#81）、コンソールはライブ表示する（#82）。**監査**: 残高への代入は**3箇所のみ**で、すべて `_credit_locked` / `_debit_locked` / `_subsidize_locked` の内側にあり、各々が台帳エントリを append する。`membership_manager` にも `add_credits` があるが、これは `lifetime_credits`＝**手数料ティア算定用の累計指標**で使える残高ではないため台帳外で正しい。**対称性も実測**: 300クレジットの購入でティア指標が 300 になり、払い戻し承認で **0 に戻る** — 返金済みの購入が**恒久的な手数料割引を買えない**ことを確認。**しかし監査は一時点の主張にすぎず、それを破る最も自然な変更が「新しい入金経路の追加」＝まさに §3-1 が行うこと**。よって**構造テスト**として固定: `self._credits[...] = ...` は台帳付きプリミティブ内にのみ存在すること、各プリミティブが `_append_ledger` を呼ぶこと、書き込み関数の集合が黙って変わらないこと。**ミューテーション検証は決済実装が持ち込みそうな変更そのもの**で行った — 課金成功後に買い手へ直接入金する `apply_payment()` を追加すると、**スイートの挙動テストは全て通過**するが本テストが `[('apply_payment', 547)]` と**名指しで検出**する。987バックエンド緑・smoke クリーン | `test` |
| 89 | **BAN と取り下げもクラッシュで取り消されていた（#88 の系統を辿った）** | #88 の直後に問答を続けた: **問**「削除だけが『約束を取り消されうる操作』なのか?」→ `grep _persist_now` で**配線は削除2箇所のみ**と判明 → 実測 → **系統全体が同じ壊れ方をしていた**。管理者が悪質アカウントを **BAN**（200 "banned"、当該アカウントの次のログインは 401）→ ディスク上のスナップショットは **`is_banned: false` のまま** → 次のティック前に **SIGKILL** → 再起動 → **BANが取り消され 200 でログインできた**。取り下げも同形で**質はより悪い**（マルウェアとして引き下げた出品が販売中に戻る）。`_persist_now` を**約束を伴う操作の系統全体**に配線: BAN・BAN解除・モデレーション取り下げ・出品復元（＋#88 の削除2経路）。**逆操作も意図的に含める** — クラッシュで解除が取り消されると**誤って罰したまま**になり、同じ不整合の裏返しになるため。**線引きは #88 のまま**: いずれも稀で保存コストは無視でき、**高頻度の書き込みには広げない**（#78 で測ったレイテンシ問題に戻る。保存は全ストアのロックを保持する）。docstring に**各メンバーと実測結果**を明記し、判定基準が「**応答が状態を約束しているか**」であって「重要そうか」ではないことを次の担当者に伝える。検証: 修正後は応答が返った時点でスナップショットが BAN を記録し、SIGKILL＋再起動後のログインが **401**。配線テストを6ハンドラに拡張し**どの呼び出し箇所を外しても落ちる**ことを確認。982バックエンド緑・smoke クリーン・E2E 11/11 | `fix` |
| 88 | **削除したアカウントがクラッシュで蘇った（「約束を取り消すクラッシュ」）** | ソクラテス問答を**4つの「オーナー判断待ち」項目の前提**に向け直した。**問**「アカウント削除の**保持ポリシー**は確かに法務判断（§3-3）。では**削除の実装そのもの**は正しいのか?」→ 実測 → **正しくなかった**。登録 → スナップショット待ち → `DELETE /api/auth/me`（**200 "deleted"**、直後のログインは 401）→ 次の30秒ティック前に **SIGKILL** → 再起動 → **そのアカウントが 200 でログインでき `/api/auth/me` がユーザー名を返した**。削除はメモリからしか消しておらず、スナップショットはユーザーを保持していた。**#75 と同型**（永続化が意図的に破壊したものを復活させる）だが**質が悪い**: 購入の消失はユーザーに見えて再実行できるが、削除は**「もう存在しない」と応答で約束**したものを製品が黙って取り消す。修正: `_persist_now()` を新設し**両方の削除経路**（本人・管理者）に配線。**消去のみを対象**とし通常の書き込みはティックを待つ（削除は稀で保存コストは実質ゼロ。高頻度操作へ広げると #78 で測ったレイテンシ問題に戻る）。保存失敗時は**沈黙**する（削除自体はメモリ上で成功済みで呼び出し元にエラーを見せてはならない。失敗は #87 の `/ready`・コンソールで可視）。**再クラッシュ試験で 401 = 約束が保たれた**ことを実測、応答が返った時点でスナップショットからユーザーが消えていることも確認。呼び出し2箇所を外すと配線テストが落ちる。982バックエンド緑・smoke クリーン・E2E 11/11。**保持ポリシー（何を残すか）と実装の正しさ（消したと言ったなら消えているか）は別物**であり、後者はオーナー判断を要さない | `fix` |
| 87 | **ソクラテス問答で発見: 保存が失敗し続けても運用者には見えなかった（#78 の「観測不能な半分」）** | 新ゴール「マスク思考法**とソクラテス問答法**で長所短所改善点を洗い出す」に基づき、**製品自身の主張を尋問**した。**主張**「#78 でスナップショットの静かな失敗を修正した」→ **問**「では今、保存が失敗し続けたら運用者はどう知るのか」→ **答: 知れない**。`_save_state_best_effort` の戻り値はオートセーブループで**捨てられ**、時刻・成否・エラーは**どこにも記録されていなかった**。ディスク満杯や権限剥奪のデプロイは30秒ごとに永遠に失敗し、ログ行以外に痕跡がない — 運用者は「永続化は有効」と信じ、**データを失った再起動で初めて知る**。#78 はクラッシュする半分を直したが、**観測不能な半分はそのまま**だった。**第2の問**「保存はアトミックだが、内容は完全か」→ **答: 保証されない**。`_durable_stores()` はストアの getter が例外を出すと warning ログのみで**黙って脱落**させ、`save()` は欠けた内容で「成功」する。修正: `_last_snapshot` が全試行を記録（ok/at/last_success_at/error/ストア数）。**ストア数は第2の問への可視化**でもある（脱落を loud にすると degraded 運用が壊れるため、失敗化せず数値で見せる — 意図的判断）。`/ready` に **#53 の two_factor probe と同型の probed チェック**を追加（失敗は実行時で、import チェックでは見えないため）→ 保存失敗で **200+degraded**（LB からは外さない）。`/api/admin/stats` は durability を**常在**させる（上のセクション群の「無ければ省略」慣行を意図的に破る — 「永続化がOFF」自体が運用者に見えるべき事実で、キー欠落は "unknown" と読め、それこそが満杯ディスクを不可視にした）。概要タブに3態カード（保存中／失敗＋損失窓／無効）。**`/health` には意図的に置かない**: Docker HEALTHCHECK は `curl -f /health` で、`/health` は `unhealthy` でも 200 を返すためコンテナ健全性に効かない。**検証手法の誤りも記録**: 最初の失敗注入 `chmod 500` は無効だった — このサンドボックスは **root で動作し、root はディレクトリ権限を無視して書ける**。`state.json` を**同名ディレクトリに置換**すると `os.replace` が root でも失敗し、実測で `ok=false`・`[Errno 21] Is a directory`・`/ready` の `missing_optional` に `durability`・`last_success_at` 保持（損失窓が見える）→ ディレクトリ除去で次周期に回復、の3態を確認。**もう一つの偽陽性**: 再開時に `ls -la \| head -4` で state dir を見て「`state.json` が無いのに ok:true＝まさに探していたバグだ」と考えたが、**`head -4` がリストを切っていただけ**で報告前に `find` で確認し誤報を回避 — **尋問の対象は自分の観測にも及ぶ**。probe を `_OPTIONAL_SUBSYSTEMS` から外すと degrade テストが失敗することを確認。978バックエンド＋69フロント、smoke クリーン、E2E 11/11 | `feat` |
| 82 | **エンフォースメントの梯子の欠けた中段（公開上限）＋金銭健全性の可視化（台帳整合性）** | §3-5 の未配線 admin エンドポイント21件のうち、**「配線して未配線リストを短くする」のではなく製品として意味のある2件**を選んだ（残る19件は API クライアント向け運用系として意図的に `leave it`）。**①公開上限** — エンフォースメントは #45（1出品の取り下げ）から #50/#58（アカウントBAN）へ直行し、**その間が崖**だった。出品を乱発するがBANには値しない出品者に対し、運営は「何もしない」か「アカウント停止」の二択しかない。公開上限は**比例的な対応**で、`-1` でいつでも解除できる（#45/#58 で確立した「破壊的操作の前に可逆性」の原則）。ユーザータブに行内コントロールを追加（`isAdmin` ガード）。**②クレジット台帳の整合性** — 「全ユーザーについて 残高 == 台帳エントリ合計」という監査不変条件で、これは**#71/#74 の復元時検査・#81 の並行性テストが依拠するのと同じ不変条件**。概要タブが「今この瞬間、金銭は健全か」に答えるようになった（整合時は✓と検査済み人数、不整合時は警告と該当ユーザーの 残高 vs 台帳合計）。**両エンドポイントともテストがゼロだった**ため5件追加 — 重要なのは2件で、**上限が実際に次の publish を止めること**（止まらない上限は飾り）と **`-1` 後に publish が再び通ること**（可逆性）。監査テストは台帳を迂回して残高を書き換え、エンドポイントがそれを検出することを確認。**意図的に直さないもの**: `set_listing_quota` は無制限化の際にハンドラから `mp._quotas` を直接触っている（`api_server.py:3384`）。動作しており、テストで挙動を固定したので、ユーザーに見える変化のないリファクタはしない。実測: 上限2 → publish 1・2 成功、3 が **400 で拒否** → 読み戻し 2/2 → 解除 → publish 3 が **200** → 整合性 `consistent: true` → **非管理者は両方 403** → ブラウザで両画面がエラーゼロで描画。959バックエンド＋68フロント、smoke クリーン、E2E 11/11 | `feat` |
| 81 | **金銭経路の並行性を初めて実測 — 3クラスとも健全、ただしテストの限界も正直に記録** | マーケットプレイスの最悪の故障はクラッシュではなく**負荷下で狂う算術**。未検証だった3クラスを実サーバーで実測: ①**二重支払い** — 1件分の残高しかない買い手が5件を同時購入 → **1件成功・4件402**、残高はきっかり0 ②**在庫超過** — `stock_limit=1` に8人が同時購入 → **1人成功**・`stock_remaining=0`・`is_sold_out=true`、負の在庫なし ③**返金の二重処理（無からのクレジット創造）** — 1件の返金申請を6並行で承認 → **1件成功・5件400**、売り手から買い手へ正確に200移動し**創造されたクレジットは0**。**3クラスとも健全**で、金銭プリミティブは並行下でも成立している。ストアレベルの不変条件テストとしてコミット（個別ケースが見逃す2件も追加: **総クレジット供給量が並行実行で不変**であること、**台帳と残高が事後も一致**すること＝復元スナップショットが検査されるのと同じ不変条件のため、ここでの競合は永続化も汚染する）。**テストの限界を docstring に明記**: `MarketplaceStore._lock` を no-op に置換し `sys.setswitchinterval(1e-6)` で再実行しても**依然として毎回1件しか成功しない**（CPython の GIL がこれら短いクリティカルセクション内で切り替わらないため）。したがってこれらは**算術を守るガード**（await を跨ぐ read-modify-write、クリティカルセクション内に追加された I/O、削除された debit）であって**ロックの存在を守るガードではない**。ミューテーション検証で判明したこの事実を伏せて「並行性を保証」と書く方が楽だったが、それは誤り。954バックエンドテスト緑 | `test` |
| 80 | **各ラウンドの E2E 検証が使い捨てで、何も守っていなかった** | 全ラウンドを Playwright で検証してきたが、**そのスクリプトは全て scratch に書かれ捨てられていた**。検証は本物だったが**後を守らない** — 1時間前に確認した導線を次の変更が壊しても、誰かが手でクリックするまで分からない（`PRODUCT_ASSESSMENT` 自身がこの欠落を記録していた）。`scripts/e2e_critical_flows.py` を新設: `smoke_api.py` が広さ（全ルート・500ゼロ）を見るのに対し、**重要導線の深さ**を実UI経由で検証する5導線11チェック（公開→公開検索に出る／UIログインがリロードを跨ぐ／**カート購入で両者の残高が実際に動く**／払い戻し申請→管理者承認→買い手が全額回復／通報がコンソールに届く）。**3番目が本題**: 監査 #44 は「有料出品が0クレジットで引き渡され売り手に入金されない」バグで、**当時の全単体テストを通過**していた（テストがレスポンスの**形状**だけを見て**残高**を見ていなかったため）。**ミューテーション検証済み**: 売り手への入金を潰すと `seller was credited the listing price -- 50 -> 50` で失敗し、戻すと通過。どのアサーションが通ったかに関わらず `/api/` 5xx と未捕捉JSエラーを常に報告。**書いていて見つかった罠も潰した**: 本スイートはセットアップ＋ブラウザで既定 `RATE_LIMIT_AUTH_PER_MINUTE=10` のほとんどを消費するため連続実行でスロットルされ、それが「壊れた導線」に見える → 429 を検出し**「スロットルであって product も suite も壊れていない」と明示して exit 3**、対処法つきで終了する。併せて `HANDOFF_INSTRUCTIONS.md` の陳腐化（「永続化なし＝再起動で消失」「全53監査エントリ」「CI制約」「コアスイート件数」）を是正 — 新セッションが最初に読む文書の誤りは以降の判断に伝播するため | `test` |
| 79 | **一次資料 `PRODUCT_ASSESSMENT.md` が12ラウンド分陳腐化していた** | `HANDOFF` が新セッションに「まずこれを読め」と指示する文書が実態と食い違っていた。§2.1 は「インメモリ永続化（**最大の制約**）」「再起動で全データが消える」のまま（#71/#74 で解消済み、残る制約は単一プロセス限定で #76 が起動拒否で強制）、§2.5 は CI ワークフローが `docs/ci/` に用意済みであることもトークン制約を実測確認したことも書いておらず、§2.6 は「フロントエンドの自動テストが**ない**」（**#52＝26ラウンド前に解消**、しかも2行後で自己矛盾）。テスト数・監査範囲・「次に着手するなら」・オーナー判断表も全て旧数値。併せて §2.7 に**直さないと決めた制約**を根拠つきで記録: スナップショットはストア単位でロックを取るためストア跨ぎ操作（チェックアウト＝marketplace で課金→cart で注文作成）が引き裂かれる理論上の窓があるが、実測で**31スナップショット中0件**、全15ストアの同時ロックは**デッドロックの危険**を持ち込むため、確率（マイクロ秒の窓×30秒周期×直後のクラッシュ）に見合わないと判断。clean shutdown 時の保存はトラフィックが止まっており常に整合 | `docs` |
| 78 | **スナップショットが実トラフィックと競合し、永続化が「静かに一度も書かない」状態だった（#74 の自己点検で発見）** | 「新機能が既存の無害な挙動に何をするか」を問う手法（#73/#75/#76 と同じ）を**自分が出したばかりの永続化層**に適用して発見した2件。**①競合**: スナップショットはストアの `_lock` を**取らずに**属性を読んでいた。エンコードはストアの dict を走査するため、並行リクエストがあると `RuntimeError: dictionary changed size during iteration` が発生。**実測: 連続書き込み下で60回中60回が失敗**。オートセーブは例外を握り潰してログするだけなので、**トラフィックのある本番では静かに失敗し続け**、運用者は「永続化が有効」と信じたまま再起動で全てを失う。修正: エンコード中はストアのロックを保持（エンコードは dataclass のフィールドを読むだけでストアに呼び戻さないためデッドロック不可）→ **60回中0回失敗**、300出品のストアで保持時間 ~9ms。**②ロックを持ったことで露出した無制限成長**: ダウンロードログはDL1件につき1エントリで**無限に増加**し、その全体が30秒ごとに再エンコードされる。**実測: 10万件で 314ms のリクエスト停止＋10MBのスナップショット、50万件で 2.5秒＋49MB**。`MAX_DOWNLOAD_LOG`（既定10万、古い順に破棄）で上限化 — これは新方針ではなく、**cart_manager の `_MAX_ORDERS_PER_USER = 1000`・user_notifications の `_MAX_QUEUE = 200` と同じ既存の判断**の踏襲。**所有権に対して安全**: 再ダウンロード権は `_downloads_by_user` インデックス由来でこのログではないため、古いエントリの破棄が購入を取り消すことは**ない**（テストで assert）。失われるのは古い分析値とダウンロード履歴の最古行のみ。**クレジット台帳は意図的に非トリム**（復元時に残高と台帳合計を突き合わせるため、切り詰めると自身の整合性検査に落ちる）。テスト3件（ロックを外すと1件失敗、上限テストは**トリムされた買い手が購入を保持し続ける**ことを証明）。950バックエンドテスト緑・smoke sweep クリーン | `fix` |
| 77 | **API経由の購入が永久に返金不能だった（§3-7 を解消。「事業判断」という当初の判定が誤りだった）** | システム全体が「**有料購入には注文が存在する**」という不変条件に依存している（返金は `order_id` をキーに持ち、注文履歴も同様）。しかし直接ダウンロード経路は**買い手に課金し売り手に入金しながら注文を作らず**、`POST /api/refunds` は `注文が見つかりません` を返し、買い手は**どの経路でも金を取り戻せなかった**。**当初「どちらの経路を正典とするかは事業判断」として §3-7 に保留したが、その枠組み自体が誤りだった** — ここに競合する2経路は存在せず、**1つの経路が他の全システムが依存する不変条件を破っている**だけ。不変条件の回復は方針選択ではなく整合性修正である。修正は**欠落していた記録を足すのみ**: 課金は既に `download()` 内で完了しているため**金銭の挙動は一切変わらない**（money path 内の変更でありながら money 挙動を触らない理由）。記録は best-effort（買い手は支払い済みで商品を受け取る権利があるため、簿記の失敗で 500 にしない）。実測: 直接API購入が120を引き落とし（550→430）**注文が記録される** → 買い手が返金申請（**201**、従来は不可能）→ 管理者が承認 → 買い手が全額回復（550）・売り手からクローバック（170→50）。**チャネル横断の二重返金ガードも維持**（同一購入に対する争議申立は 400 で拒否され、クレジットは動かない）。テスト4件（**記録を外すと2件が失敗**）: 有料DLがクローバックの読む `owner_id`/`final_price` を持つ完了注文を作る／無料DLは作らない／無料再DLは2件目を作らない／カートマネージャが壊れていても商品は返る。947バックエンドテスト緑・smoke sweep クリーン | `fix` |
| 76 | **複数ワーカーが黙ってデータを破壊していた（`--workers` は本番の標準起動）** | `uvicorn/gunicorn --workers N` は本番の**標準的な起動方法**だが、ストアはプロセス内 dict のため「同じサービスがスループット増」ではなく「**メモリが分断された複数のサービスが同じポートに応答**」を意味する。誰も止めていなかった。**実測**（`--workers 2`）: `/api/auth/register` で12アカウント中9件が作成され、**そのうち即座にログインできたのは3件だけ**。各アカウントは片方のワーカーのメモリにしか存在せず、ログインは応答したワーカーに当たるため。**ユーザーは登録に成功した直後にサインインできず、しかもどこにもエラーが出ない**。永続化がこれを「壊れている」から「破壊的」に格上げした: 全ワーカーが同じ30秒周期で**同一ファイルにストア全体を上書き**するため、最後の書き手が他の全ワーカーの成果を**永久に消す**。ドキュメントには「単一プロセス限定」と書いてあったが、**ドキュメントはガードではない**。修正: 先頭プロセスが state ディレクトリに**排他 advisory ロック（flock）**を取り、2つ目のプロセスは**起動を拒否**して対処法をメッセージに明示（「--workers を外すか 1 にせよ」）。破損スナップショットで起動を拒否するのと同じ理由 — **分断された状態でトラフィックを捌くより、起動しない方がまし**。検証: `--workers 2` が拒否ログを出して終了（従来は起動して破壊）、単一ワーカーは不変（health 200・永続化ONで smoke sweep クリーン）、clean shutdown でロックを解放し再起動で再取得。テスト3件（**実サブプロセス**で2つ目がロックを取れないことを確認）。943バックエンドテスト緑。**注意**: これは複数ワーカーを「動くように」するものではない（それには共有ストアが必要＝§3-4 に残る唯一の技術的未解決項目）。**黙って壊すのを止めた**ものである | `fix` |
| 75 | **失効済みトークンが再起動で復活した（#71/#74 が作り込んだセキュリティ後退）** | 永続化を入れたことで、**無害だった省略が実際の穴に変わった**。`verify_access_token()` は jti が失効リストにあるトークンを拒否するが、そのリストは「一時的」としてスナップショット対象外にしていた。`COCOA_JWT_SECRET` が安定している本番構成では、**再起動前に発行されたトークンは再起動後も暗号的に有効**。永続化以前は再起動で全アカウントが消えたため当該トークンは誰にも解決せず無害だったが、**アカウントが残るようになった結果、アカウントは在るのに失効情報だけが消える**状態になった。**実測で再現**: ログイン → ログアウト（401を確認）→ SIGTERM → 同一 state ディレクトリで再起動 → **ログアウト済みトークンが 200 を返す**。これは #73（永続化が回復可能な管理者降格を永久ロックアウトに変えた）と**同じ形**であり、**永続化の出荷はデータ消失で隠れていた挙動の深刻度を変える**という教訓の2例目。修正: auth ストアの除外を**全廃**し、失効リスト・reset/verify トークン・**APIキー**を全て保存対象に。APIキーも同じ「一時的」理由で除外していたが、**他の所有物が全て deploy を生き延びる今、キーだけ黙って失効するのは不整合**だった（実測でキーが再起動後も認証できることを確認）。除外を残すのは**設定値のみ**（通知・保存検索の上限＝環境変数から起動時に読む値で、古いスナップショットが現行設定を上書きしてはならない）。回帰テスト3件、**除外を戻すと2件が失敗**することを確認。940バックエンドテスト緑 | `fix` |
| 74 | **全ストアの永続化 — 再起動がマーケットプレイスを消さなくなった（§3-4 の本体を解消）** | §3-4 は当初から「全ビジネス状態がプロセス内メモリ＝再起動で全消失」を記録し、**58ラウンド以上「データモデル凍結を伴う事業判断」として保留**されてきた。#71 で最小整合単位（アカウント＋金銭）を救出したが、残り13ストア（出品・カート・注文・ウィッシュリスト・コレクション・コミッション・ライセンス・ギフトカード・紹介・保存検索・会員・払い戻し・通知・バンドル）は消え続けていた。**保留理由の再検証（マスク第1歩）**: 実際の障害は事業判断ではなく**技術問題**だった — 各ストアの `to_dict()` は**表示用の非可逆シリアライザ**で、`MarketplaceListing.to_dict()` は **`parameters`（買い手が金を払った商品そのもの）** と `rating_sum` を落とす。`to_dict()` ベースで移行していれば**購入物を静かに破壊する**データ喪失バグになっていた。バージョン付きスナップショットは何も凍結しない（フィールド追加は既定値で旧スナップショットが読める）ことも #71 の `UserRecord` 汎用直列化で実証済み。**解決**: `main/state_codec.py` — `to_dict()` ではなく**生きたオブジェクト**を往復させる型タグ付きJSONコーデック（datetime・set・tuple・**タプルキーの辞書**〈`_purchase_seller`〉・**タプル内datetime**〈`_download_log`〉・**タプルの集合**〈`_refunded_purchases`〉・ネストした dataclass に対応）。**pickle を採らなかった理由**: 敵性スナップショットの unpickle は RCE だが、本コーデックは**明示レジストリに載る dataclass しか構築できない**ため最悪でもデータ破損に留まり、かつ人間が読んで修復できる。ストアは `vars()` 経由で捕捉するため**属性を足し忘れて静かに失われる**事故が構造的に起きない。`main/state_snapshot.py` は15ストアを**1ファイルにアトミック書き込み**（分割すると 10:00 のカートと 10:05 の出品を突き合わせる不整合が起きる）。**併せて処理**: ①検索インデックスは派生状態のため保存せず**復元後に出品から再構築** — さぼると「出品はあるのに空のカタログを配信」＝ #47 が撲滅したはずの障害偽装が再発する ②#71 のクレジット台帳整合性検証を**汎用復元後に再適用**（一般化で保証を失わない） ③#71 世代のディレクトリ（`user_state.json`＋`credit_state.json`）を**初回起動時に自動移行**（アップグレードでアカウントを落とさない） ④`state_snapshot` は `state_codec` を**相対import優先＋フラットfallback**（HANDOFF §1.1 の本番503を2回起こしたパッケージング罠）。検証: **実プロセス2台の再起動** — 有料出品を公開しカート経由で購入・レビュー → SIGTERM → 同ディレクトリで2台目起動 → **残高520・注文1・レビュー1・検索ヒット2が完全一致**、買い手は再ログインでき、**再ダウンロードで購入済みパラメータ `{"Hair":0.5,"Eye":1}` が無傷**（＝`to_dict()` 移行なら失われていたデータ）。#71世代ディレクトリの移行とログイン継続も確認。937バックエンド（新規14）＋64フロント緑、build/lint クリーン、`COCOA_STATE_DIR` 未設定の227ルート smoke sweep は不変。`.env.example` の陳腐化（#60 で削除済みの i18n キー群・「~95モジュール中60が死」ヘッダ）も是正 | `feat` |
| 73 | **管理者が全管理者を永久ロックアウトできた（実バグ）＋ロール管理UI・認定取り消し** | 「未配線の admin エンドポイント（ロール変更）を配線する」ラウンドとして着手したが、**要件を疑った結果（マスク第1歩）、本当の必要は「配線」ではなく「安全性」だった**。`change_role()` のガードは呼び出し元ロール・ロール名・対象存在のみで、**自己降格ガードも最後の管理者ガードも無かった**。**実測で再現**: 唯一の管理者が自分を降格 → 管理者数 **1→0** → 同アカウントは `change_role` を呼べず**回復不能**。単に破壊的なだけでなく**不可逆**である理由が3つ揃っていた: ①降格されたアカウントはもう `change_role` を呼べない ②起動時ブートストラップは `register("admin",...)` の `ValueError` を握り潰すため再起動しても role は戻らない ③**#71 の永続化により降格がスナップショットに保存され、再起動を跨いで永久化**（以前は全データ消失で偶然復旧できたがそれも消えた）。回復手段はスナップショット手編集か全データ破棄のみ。**見落としである証拠**: 同一ファイルの `ban_user()` は #49 で既に自己BANガードを得ている — ロール降格は同じロックアウトの別経路であり、より重い。修正: `_actor_id()`（#49 由来）を再利用し **自分のロール変更を一律禁止**（自己昇格も同じ経路のため）＋ **最後の管理者の降格を禁止**（`UserStore.count_admins()` を新設しロック内で計数）。**併せて判明した2件**: ①ハンドラが `AuthError` と `ValueError` を一括400にしていたため、モデレーターが admin 限定ガードに弾かれても「不正なリクエスト」と報告されていた → 拒否は **403**・対象不在は **404**・未知ロール名のみ **400**（同じ二重ゲートの verify-creator と同形に統一） ②**クリエイター認定バッジが取り消せない** — 付与は申請審査から可能だが `DELETE .../verify-creator` がフロントから到達不能で、#45（取り下げの復元）・#58（BAN解除）で確立した可逆性の原則が未適用だった。UI: ユーザータブにロール選択とバッジ取り消しを追加。**両方 `isAdmin` で囲む**（タブ自体はモデレーターにも開いたままだがこの2操作はサーバー側 admin 限定 — 停止中ユーザータブと同じ不一致回避）。**自分の行には操作を出さない**（常に403になるボタンを見せず、UIをサーバーのガードと一致させる）。ロスターに `is_creator_verified` を追加しバッジがある行にのみ取り消しを表示。`ROLE_LABEL` から誤った `creator` を削除（`ROLES` は user/moderator/admin のみで creator はフラグ）。**死コード削除**: `marketplaceService.getFeatured()` は呼び出し元ゼロ・featured ストリップも無く・キュレーション状態は永続化対象外で再起動で消える。ランディングには既に4つの発見ブロックがあり5つ目の手動キュレーション面は「完成」ではなく新機能のため**関数を削除**（#57 のお気に入りと同じ「過剰」判定。バックエンド3エンドポイントは API クライアント向けに残す）。検証: 新規13テスト（ガード層7・ハンドラ層6）、**ガードを外すと3件が失敗**することを確認。923バックエンド＋64フロント緑、build/lint クリーン。実E2E: **最後の管理者の自己降格 403**・**モデレーターのロール変更 403**・管理者がUIから他アカウントを変更しサーバー側も一致・**自分の行に操作が出ない**・`/api/` 4xx/5xxゼロ・JSエラーゼロ。smoke sweep も予期しない5xxゼロを維持 | `fix` |
| 72 | **#71 の永続化が、実際に出荷しているデプロイ形態（docker compose）に未配線だった** | `COCOA_STATE_DIR` はオペレーターの opt-in としてのみ存在し、このリポジトリが実際に出荷するデプロイ手段である docker compose（#18）は依然として**コンテナ再作成のたびに全アカウント・全残高を消失**していた。配線: compose の api サービスに `COCOA_STATE_DIR=/app/state` と新規 named volume `cocoa_state` のマウントを追加（postgres/redis/prometheus/grafana の既存 volume 群と同列）。Dockerfile では **`chown -R` より前に `/app/state` を作成** — Docker は fresh named volume をイメージのマウントパスの内容**と所有権**から初期化するため、存在しないパスに volume を被せると root 所有となり、非rootの `cocoa` ユーザーがスナップショットを書けなくなる（順序が本質）。検証: `docker compose config` で env 解決・`/app/state` へのマウント・volume 宣言を確認。実 `docker build` はサンドボックスの egress ポリシーが Docker レジストリを遮断するため実行不能（#18 と同じ記録済み制約） | `feat` |
| 71 | **オプトイン永続化 — 「アカウント＋金銭」という最小の整合単位が再起動を生き延びる** | §3-4（全ストアがプロセス内メモリ＝再起動で全消失）は58ラウンド「データモデル凍結を伴う事業判断」として保留してきたが、ユーザーが永続化を欠落として提示された後もゴールを再確認したため、**事業判断を伴わない形（既定オフ・ベンダーなし・スキーマなし）**で実装。**発見**: `MarketplaceStore` には `export/import/save/load_credit_state`（バージョン付き・台帳整合性検証・`mkstemp+fsync+os.replace` のアトミック書き込み・ブート時ブラインドロード設計）が**完成品として存在し、呼び出し元がゼロ**だった — #46/#68 と同型の「実装済み・未到達」が、よりによって製品で最も重要なデータで起きていた。**計画の自己修正（マスク第1歩を自分の計画に適用）**: 金銭のみの永続化は**不整合** — 残高は user_id をキーに持つため、アカウントが蒸発したプロセスに残高だけ復元しても「誰もログインして請求できない孤児マネー」になる。当初計画の「auth は保留」は要件の誤りだったと判定し、`UserStore` に鏡像API `export/import/save/load_user_state` を追加（`dataclasses.fields` による全フィールド汎用直列化＝将来フィールド追加後も旧スナップショットが読める、username/email インデックスは派生状態として再構築）。**意図的に永続化しないもの**: APIキー（長寿命credentialのため独立判断として §3-4 に記録）、分単位寿命の reset/verify トークン、revoked-jti（アクセストークンは1時間で自然失効）。**配線**: `COCOA_STATE_DIR` 未設定（既定）で現行とビット単位で同一、設定時は startup ロード＋clean shutdown セーブ＋**30秒オートセーブスレッド**（SIGKILL/電源断の損失窓を上限化。書き込みがアトミックなのでクラッシュ中の保存でも旧スナップショットが無傷）。**fail-closed**: 破損スナップショットでは起動を拒否 — 残高ゼロ・ユーザー空で静かに開店することは #47 アンチパターンの金銭版であるため。スナップショットは mkstemp 由来の **0600**（user_state.json はパスワードハッシュを含む）。検証: 配線テスト7件（既定は no-op／登録→保存→**新ストア**→ロード→**同一人物がログインでき残高250が無傷**／ファイル不在は正常起動／クレジット・ユーザー両方の破損スナップショットが**何もコミットせず起動拒否**／ファイルモードに group/world ビット無し／スレッドの起動と停止）。**実プロセス2台のE2E**: uvicorn A で登録＋250付与（残高300）→ SIGTERM → 両スナップショットがディスクに 0600 で存在 → 同じディレクトリで uvicorn B → 起動ログに両リストア → **同一ユーザーがログイン（200）し残高300** — 71ラウンドで初めて、再起動が「誰であるか」と「何を持っているか」を消さなくなった。既定経路は不変: 910コアテスト緑、env 未設定の227ルート smoke sweep で予期しない5xxゼロ | `feat` |
| 70 | **#47 の「空の200」取り残し3件 — うち1件は稼働中のフィード画面** | #47 は「依存が壊れているのに空リストを返すと、本当に0件なのか区別がつかない」アンチパターンを52エンドポイントから除去したが、**3件が旧来の形のまま残っていた**。全ハンドラを走査し `if not get_x: return <空>` 形のガードを機械的に抽出して発見（`get_security_report` の1件は成功枝のため偽陽性と判定、#54 で既に503化済み）。取り残し: `/api/auth/feed`（`creator_feed`）・`/api/tags/feed`・`/api/marketplace/favorites`。**重大なのは1件目**で、これは**フィード画面が実際に呼んでいる**エンドポイント（`userService.ts`）。auth または marketplace が落ちていると、ユーザーには「**フォロー中のクリエイターは何も公開していない**」と表示され、**障害が完全に隠蔽**される。3件とも 503 に統一。**正当な空は変更しない** — 誰もフォローしていないユーザーは引き続き 200＋空リスト（それは「真実の答え」であって障害ではない）。テストは両面を固定: 各エンドポイントがどちらのサブシステム欠落でも503を返すこと＋**フォロー0人は依然として空の200**であること。旧形では6アサーションが失敗することを確認。903バックエンドテスト緑、227ルートのsmoke sweepは予期しない5xxゼロを維持、Chromiumでフィード画面が空状態を正しく描画しエラーゼロ | `fix` |
| 69 | **「誰も開けないキュー」系統を機械的に洗い出す仕組み＋購入争議の調査（結論: 実装しない）** | このリポジトリの反復的な欠陥はクラッシュではなく**行き止まり**（サーバー側は完成しユーザーはデータを投入できるが、製品側にそれを取り出す手段が無い）である: #46 出品通報・#49 クリエイター申請・#68 コミッション紛争。いずれも**手作業で・遅れて**発見された。`scripts/unwired_endpoints.py` を追加し、**バックエンド218エンドポイントとフロントエンドの実呼び出しを機械的に差分**して一覧化（現在: 配線済み169・未配線49）。テストファイルは除外（テストにしか出てこないパスは製品に配線されていない）。**未配線＝バグではない**ことを出力とdocstringで明示し、`wire it`（#46/#68 型の行き止まり）/ `delete it`（#57 のお気に入りのように過剰な部品）/ `leave it`（意図的にサーバー専用）の3択を意識的に選ぶための道具と位置づけた。**この道具を使った最初の調査＝購入争議 `POST /api/marketplace/{id}/dispute`（両端未配線）**: 実測の結果 **直接ダウンロードは注文を作らず**（買い手550→430・売り手+120・orders=0）、払い戻しは `order_id` 必須のため **API直叩きの購入は払い戻し不能**と判明。ただし**フロントエンドは有料の初回購入を必ずカート経由にしており**（`ListingDetail.tsx`「paid first-time purchases go through the cart」、直接取得は無料/所有済みのみ）、**UI経由では発生しない**。よって「第2の返金経路」をUIに足すと既存の払い戻しと重複し二重返金リスクを増やすだけと判断し、**意図的に実装しない**。残るのは「APIレベルでは払い戻し不能な課金経路が存在する」という**金銭モデルの不整合**で、どちらの経路を正典とするかは**事業判断**のため §3 の保留項目として記録するに留める（推測で金銭挙動を変えない） | `chore` |
| 68 | **コミッション紛争が「誰も開けないキュー」で滞留していた（#46 と同型の行き止まり）** | #31 で受注制作の当事者が「問題を報告」できるようにし、`moderation_queue` に `commission_dispute` として積まれるようにしたが、**フロントに `/api/admin/moderation` への参照が1件も無く**、金銭と納品物が絡む紛争が**永久に未処理のまま滞留**していた（#46 の「実装済み・到達不能な裁定キュー」と同型）。**マスク流に「作る前に要件を疑う」**: キューは `listing_report`／`review_report`／`creator_application` も mirror しているが**その3つは既に専用タブを持つ**ため、汎用キューUIを作ると全件が二重表示になる。実際に他に居場所が無いのは `commission_dispute` **1種類のみ**と特定し、**その1種類だけにスコープを絞った**（不要な部品を作らない）。新規「コミッション紛争」タブ: 理由・詳細・優先度・ステータスのバッジ、申立日時、キュー健全性タイル、対象コミッションへのリンク、記録済みメモを表示。操作は優先度引き上げ・自分が担当する・確認中にする・解決/却下。**解決には理由の入力を必須**とした（#46 と同じ規則。理由なき裁定は次の担当者が経緯を再構築できない）。実サーバー＋Chromium で E2E 検証: APIで申し立てた紛争が理由・詳細つきで表示され、**理由が空の解決は拒否されて未処理のまま**、自分への割り当てが機能し、理由付き解決後は**サーバー側が `status=resolved`・メモ・`assigned_to` を保持**。`/api/` 4xx/5xx ゼロ・JSエラーゼロ。バックエンド変更なし、build/lint クリーン、フロント62テスト（kindフィルタ・status PUT ボディ・priority PUT ボディを固定する3件を新規追加） | `feat` |
| 67 | **一次資料 `PRODUCT_ASSESSMENT.md` が24エントリ分陳腐化していた（完了項目を「短所」と記載し続けていた）** | 同文書は「プロダクトの現状」を伝える一次資料でありながら**最終更新が #42 時点**で、その後の24エントリを反映しておらず、**既に解消済みの項目を現在の短所として記載し続けていた**: §2.3「メール送信がない」（**#51 で解消**）、§2.4「管理者 UI がない」（**#46/#49/#50/#58/#62/#63 で7タブのコンソールを実装済み**）、§3 改善案表の P2 2件（同）。新規セッションがこれを読むと**存在しない欠落を埋めようとして重複実装する**。全面更新: ヘッダに**実測された健全性**（API 227ルートに予期しない500ゼロ／フロント32ルートで未捕捉JSエラー0・API 5xx 0・空白ページ0／バックエンド2,101テスト0失敗／フロント59テスト・build・lint緑／`main/` が33モジュールと1対1）を明記、§1 の数値を実測値に修正（225→**227ルート**、35→**37ページ・32ルート**、約1,200→**2,101テスト**、42→**66監査エントリ**）、§1.4 の実バグ表に #44・#49・#54・#64・#65 を追加、§2.3/§2.4 を解消済みに、§4「次に着手するなら」を**「事業判断を要さない主要項目は出尽くした」**という現実に合わせて書き換え、**オーナー判断待ちの5項目（永続化・実決済・出金・削除ポリシー・CI導入）を「製品を商用サービスに変える最後の距離」として一覧化**。文書のみの変更（コード変更なし） | `docs` |
| 66 | **バグを見つけた手法を再現可能にした（マスク第4・第5ステップ「サイクルタイム短縮」→「最後に自動化」）＋全面スイープで健全性を実測** | #64・#65 の**実バグ2件はどちらもコード読解ではなく「実サーバーに全ルートを機械的に投げる」ことで発見**した。両方ともフロントからもテストからも呼ばれないエンドポイントにあり、**単体テストはモックのため素通り**していた。この手法を使い捨てにせず `scripts/smoke_api.py` として恒久化: 宣言済み**227ルート全部**に実リクエストを投げ、**予期しない5xxがあれば exit 1**。`503`（サブシステム不在の正直な報告＝#47 の規約）は許容し `500` は常に失敗とする。実在オブジェクト（出品・ライセンス）を先に生成してからパスパラメータを埋めるため、not-found 分岐だけでなく**実処理の経路**に到達する。**ガードが実際に発火することを検証**: #64 のバグを一時的に戻すと `500 POST /api/admin/licenses/{key_id}/revoke` を検出し **exit 1**、修正状態では **exit 0**。あわせて本ラウンドで製品全体の健全性を実測: **API 227ルート（GET 108＋書き込み119）に予期しない500ゼロ**、**フロントエンド32ルートをPlaywrightで巡回し 未捕捉JSエラー0・API 5xx 0・空白ページ0**（唯一のconsole 404は #33 で意図的に「申請なし」として扱う既知の応答）。手順は `HANDOFF_INSTRUCTIONS.md §3.1b` に記載 | `chore` |
| 65 | **`/api/avatars` がDB未構成時に3通りの嘘をついていた（500／空の成功200／偽の作成成功）** | #64 と同じ「実欠陥を探す」方針で、**実サーバーに対し全108本のGETルートを機械的にsmokeプローブ**して発見（コード読解ではなく実測）。`database_manager` は SQLAlchemy 無しでも import は通るが、セッション呼び出しで `NameError: sessionmaker` になる。`api_server` は同モジュールが**既に公開している `SQLALCHEMY_AVAILABLE` フラグを一度も参照しておらず**、DB未構成デプロイで3通りの異なる不正直な応答をしていた: ①`GET /api/avatars/{id}` → **500**（既知の構成状態をサーバーのバグとして報告し、#10/#12 で計装した5xxエラーバジェットを汚染＝#53/#54 と同型）②`GET /api/avatars` → **200 `{"avatars":[],"status":"success"}`**（`get_user_avatars()` が全例外を握り潰して `[]` を返すため、**障害と「アバターが0件」が区別不能**、しかも `status:"success"` と明示的に成功を主張＝#47 が52エンドポイントから除去したアンチパターンの取り残し）③`POST /api/avatars` → **200 `{"status":"created_mock"}` ＋新規uuid**（**永続化されていない書き込みを成功として報告**し、クライアントは何も指さないIDを保持する＝データ損失の罠）。3本とも理由付き **503** に統一（#47 の「サブシステム不在は503」規約に一致）。`SQLALCHEMY_AVAILABLE` は既存の `PROMETHEUS_AVAILABLE` と同じ capability-flag パターンで import・フォールバック定義。**修正後に同じ108本sweepを再実行し 500 はゼロ**、残る5xxは正直な503（DB未構成・prometheus_client未導入）のみ。テストは3本すべてが503を返すこと＋固有の2退行（空の成功を返さない・偽のIDを作らない）を固定し、ガード除去で全て失敗することを確認。957バックエンドテスト緑 | `fix` |
| 64 | **管理者のライセンス失効が全呼び出しで 500（払い戻し・BAN と並ぶエンフォースメント手段が到達不能）** | 「機能の追加」ではなく**実際の欠陥**を探す方針で金銭・強制執行系の経路を精査中に発見。`POST /api/admin/licenses/{key_id}/revoke` が `admin["sub"]` を読んでいたが、依存の `get_current_admin` が返すのは**正規化済みペイロード**（`{"user_id","username","role"}`）で、`"sub"` を持つ生JWT形状は `_verify_token()` 経由のみ。`KeyError` はハンドラの `except ValueError` をすり抜け、**全呼び出しが HTTP 500**。しかも代替経路が無い: もう一方の失効ルート `POST /api/licenses/{key_id}/revoke` は**発行者限定**で鍵の保持者には 403 を返すため、**管理者が発行済みライセンスを失効させる手段が製品上ゼロ**だった。#49（auth_manager で reviewer/banned_by が記録されない）と**同一の形状取り違え**。全ハンドラを AST で静的走査した結果、**残る交差はこの1件のみ**（他の `payload["sub"]` は `_verify_token()` の戻り値に束縛されており正しい）。修正前に実サーバーで再現（publish→download→ライセンス発行→管理者失効＝**500**、サーバーログに `KeyError: 'sub'`）、修正後は同一フローが **200**・`revoked_by` に管理者の実IDを記録・鍵が `is_revoked=true` に反転・非管理者は依然 **403**。テストは①エンドポイント単体（管理者IDが `revoke_key` に届くことを assert）②**クラス単位の静的ガード**（`Depends(get_current_user/admin)` を既定値に持つ引数から `"sub"` を読むハンドラが1つでもあれば失敗）で**この系統の再発を封じる**。両テストとも修正前のコードで失敗することを確認。942バックエンドテスト緑 | `fix` |
| 63 | **管理コンソールに概要（プラットフォーム統計）が無い** | `GET /api/admin/stats`（ユーザー＝総数/ロール別/有効/ロック、マーケットプレイス＝公開出品/総DL/総評価/カテゴリ数）は完成済みだがフロント未接続で、コンソールは開くといきなり通報キューが表示され**プラットフォーム全体の一望**ができなかった。モデレーションコンソールに「概要」タブを追加し**既定のランディング**に。各セクションは対応サブシステムが報告した場合のみ描画（`AdminStats` 型は #47 のサーバー側条件付き組み立てに合わせ各セクションを optional 化）。`get_current_admin`（admin/moderator）で他のキュータブ同様に両ロール表示。検証: build/lint クリーン、フロント59テスト（`GET /api/admin/stats` を固定するサービス契約テスト1件を新規追加）。実ルーティングでの確認: ペイロード形状が型と一致（`users.by_role`・マーケットプレイス各カウント）、**非管理者は 403** | `feat` |
| 62 | **管理者のユーザー一覧・クレジット付与に UI が無い（未接続 admin エンドポイント）** | `GET /api/admin/users`（ユーザー名簿）と `POST /api/admin/credits/grant`（クレジット付与）はバックエンド完成・テスト済みだがフロント呼び出しゼロで、§3-5 に「残りの未接続 admin エンドポイント」として記録していた欠落の一部。マーケットプレイス運営者は「アカウントを探して補償クレジットを付与する」という基本的なサポート操作ができなかった。**付与は実金銭ではない**: #46 の払い戻し承認が動かすのと同じアプリ内クレジット台帳への操作で、サーバーが1回の上限を強制し台帳に記録する（§3 の保留＝実決済/出金とは別物）。モデレーションコンソールに「ユーザー」タブを追加: `GET /api/admin/users` の名簿（エンドポイントは全件返すためユーザー名・メール・ID・ロールで**クライアント側検索**）、各行にロール/停止中/ロック中バッジ・登録日・最終ログイン・失敗回数、行内のクレジット付与フォーム（正の整数をクライアント側で検証、上限はサーバーが強制）で付与後の残高を表示。`get_current_admin`（admin **または** moderator）で通る2エンドポイントなので reports/refunds タブと同様**両ロールに表示**（BAN タブのみ admin 限定を維持）。`AdminUser` 型・`listUsers`/`grantCredits` サービス追加。検証: build/lint クリーン、フロント58テスト（GET パスと `{user_id, amount}` POST ボディを固定する**サービス契約テスト2件**を新規追加）。実ルーティングでの E2E: 管理者が名簿取得（対象在籍を確認）・**非管理者は 403**・250 付与で `new_balance` 300・0 付与は 400 で拒否 | `feat` |
| 61 | **通報者に処理結果が返らない（信頼ループの片側が開いたまま）** | 数ラウンドかけて築いた信頼ループ（通報→裁定→取り下げ/復元→出品者履歴→BAN）に**通報者側の閉包が無かった**。裁定時に通知が飛ぶのは**出品者**（取り下げ時）だけで、**通報した本人は結果を一切知らされない**。モデレーションの技術情報も関連ソフト（Booth/Gumroad/Amazon 等）も一貫して「通報には結果通知を返す」— 通報者の信頼を築き、重複・再通報を減らすため。両 resolve エンドポイント（出品通報・レビュー通報）に共有ヘルパ `_notify_reporter_of_outcome` を追加し `report.reporter_id` へ通知。**公開するのは結果カテゴリのみ**（resolved＝確認し対応／dismissed＝確認したが違反なし）で、**内部メモ `resolution_note`（#46 の内部引き継ぎ記録）は絶対に含めず**、被通報者にどんなエンフォースメントが及んだか（＝当該ユーザーのプライバシー、かつ報復回避）も明かさない。自己通報（通報者＝裁定者）はスキップ、通知失敗はモデレーション応答に影響させない（best-effort）。新規 mutable kind `report_reviewed`（常時オンの `system` と別に、この儀礼的通知を通報者が個別にミュートできる）を追加し通知設定UIに露出。テスト6件（resolved/dismissed の文言・**内部メモ非漏洩**・自己通報スキップ・ミュート抑止・レビュー通報のフォールバック表記）。ミューテーション（通知を no-op 化）で肯定4件が失敗し否定2件は緑のまま＝テストが実効的であることを確認。実 FastAPI ルーティングでの E2E: publish→report→admin resolve→通報者が listing 名付き `report_reviewed` を受信し内部メモ漏洩なし。バックエンド719＋フロント56緑、build/lint クリーン | `58ffe02` |
| 60 | **死コード削除ウェーブ2 — `main/` を「動く製品が import する 33 モジュール」に一致させた（イーロン・マスク「部品を削除せよ」の完了）** | #59 は `main.api_server` **＋ tkinter ランチャー `main.py`** を起点に閉包を取ったため、ランチャー自体とそこからしか辿れない部品が生き残っていた。今回**製品の唯一の実体である `uvicorn main.api_server:app` だけ**を起点に閉包を再計算（`_import_subsystem("X")` の文字列動的 import も AST で解決）した結果、43 モジュール中**生存33・到達不能10**。到達不能10（`config`〈製品はenvを直読みし誰もimportしない中央設定〉・`config_validator`・`dependency_injection`・`grafana_integration`・`i18n_manager`〈フロントが独自にi18n〉・`logging_config`・`logging_manager`・`main`〈tkinter GUIランチャー＝製品ではない〉・`security_manager`〈#59で保持したが実は死んだ i18n_manager/vrchat_sdk からしか参照されない孤島。live の security は別物の `integrated_security`〉・`vrchat_sdk_integration`）を削除。併せて**死コードのみを検証していた12テスト**（うち `test_cocoa.py` は PerformanceMonitor 部分が専用の `test_performance_monitor*.py` と重複、残りは死んだ LoggingManager と空辞書アサート）、**`services/` マイクロサービス・スキャフォールド一式**（未デプロイ、死んだ `config` を参照）、`scripts/expand_languages_2025.py`（死んだ i18n_manager を import）、**`test_integration_2025.py`**（#59 で削除済みの advanced_security_2025/nft/rag/vr_ar を import する**壊れたまま残っていたストラグラー**）を削除。`tests/conftest.py` の死モジュール参照（`main.config` fixture・`dependency_injection`/`logging_config` の巻き戻し・`main.py` 横取り回避ヘルパ＝ランチャー削除で問題自体が消滅）も除去。安全性: 33 live のどれも削除10モジュールを import しないことを閉包で確認、リポジトリ全体で削除モジュールへの残存 import ゼロを grep 確認、`main.api_server` が boot し 233 ルート生成、core-4 スイート**890件緑**、フルスイート（2,090件）の失敗集合は #59 の 1失敗/5エラー→**0失敗/3エラー**に縮小（残る3件は pytest/_cffi_backend 未導入の既存環境要因のみ、`No module named` 系ゼロ）。これで `main/` は**製品が実際に使う33モジュールと1対1**になり、PRODUCT_ASSESSMENT が最大の弱点として記録し続けた「死コード」項目は解消 | feat |
| 55 | **認可の体系的監査（BOLA/BFLA）— サーフェス健全性を確認、横断分離テストで固定** | #54 が偶然発見した認可欠落を受け、OWASP API Security Top 10（API1:2023 BOLA / API5:2023 BFLA）に基づき **139 の user/security-gated エンドポイント全体**を体系的に監査した（当初サブエージェント並列ワークフローを組んだが週次上限で失敗 → インラインで実施）。手法: ①id を受け取る全ハンドラを機械抽出し、**呼び出し元IDを本体で参照していない**もの（BOLA 候補）を自動フィルタ → **候補1件のみ**（`GET /reviews/{id}/replies`＝公開データのコメント列で偽陽性）。②最高リスクの id 受け取りエンドポイント約15本（orders・commissions・collections・marketplace analytics/licenses・licenses activate/revoke・saved-search・notifications・promo-codes）を手動精査 → **全て所有者/当事者チェックを実施**（`current_user["user_id"]` をマネージャに渡し scope、または `PermissionError`→403、commission は明示的 party チェック）。③`security` 依存の `/api/admin/membership*`・`/api/admin/refunds*` はハンドラ内で `_verify_token` → マネージャの `_require_admin(payload)`（role ∉ {admin,moderator} で PermissionError）を確認 → **BFLA なし**。結論: **認可サーフェスは健全**で、アプリ全体が「呼び出し元IDをマネージャに渡し scope/enforce する」統一パターン（OWASP 推奨の consistent authorization module）を適用済み。#54 は `/api/` 規約の外にある運用エンドポイントという**孤立した例外**であり氷山の一角ではなかった。監査を恒久化するため、4マネージャ横断の**クロスユーザー分離回帰テスト5件**を追加（他人の保存検索削除→404かつ残存、他人の通知既読→404かつ残存、get_order が呼び出し元IDを store に転送、commission の非当事者→403・両当事者→許可）。commission の party チェックを除去すると分離テストが失敗することをミューテーションで確認。**コード変更なし（テスト＋文書のみ）**、153テスト緑 | docs |
| 54 | **運用エンドポイント3本が一般ユーザーに開放されていた（Broken Function Level Authorization）** | #53 の §3-6 で「構成状態を500で報告」候補として挙げた `GET /backups`・`GET /security/report` を実サーバーで検証したところ、**より重大な問題**が判明: 3本（`/metrics` JSON版も同様）が `Depends(get_current_user)` のみで、**任意のログイン済みユーザーが 200 で運用情報を読める**（システム性能・バックアップ台帳のID/サイズ/時刻・脅威レベル/ロックアウト数/不審行動統計）。これは OWASP API Security Top 10 の **API5:2023 Broken Function Level Authorization**（管理者向け機能が一般ユーザーに露出）に該当。この環境ではフラット import 失敗で管理系モジュールが None のためスタブ（空/`threat_level: low`）が返るが、**uvicorn 実行時は管理系が import 成功し実データが返る**（＝本番で情報漏洩）。修正: 3本を `Depends(get_current_admin)` に変更。**監視は影響なし** — Prometheus のスクレイプ先は認証不要の別エンドポイント `/metrics/prometheus`（`docker/prometheus.yml`）、コンテナの HEALTHCHECK は `/health`、どちらも不変。フロントは3本とも未使用。あわせて、管理系サブシステム不在時に**偽の安心（`threat_level: low` / 空の `status: ok`）を返さず 503** にした（#47/#53 の静かな劣化アンチパターン。真の例外による 500 経路は保持）。実サーバー検証: 一般ユーザーは3本すべて **403**、管理者 200、未認証 401、`/metrics/prometheus` と `/live` は開放のまま。単体テストは**各ルートの依存ツリーに `get_current_admin` が配線されていること**（`/live` を否定対照に）・ゲートが一般ユーザーを拒否し admin/moderator を通すこと・サブシステム不在で 503 を返すことを検証。新規7件中6件は修正前に失敗。931バックエンドテスト緑 | `b2ea4fc` |
| 53 | **未構成の任意機能を「サーバー障害(500)」として報告していた（2FA）** | #51 のE2E中に**実測で観測**: `COCOA_2FA_SECRET` 未設定のデプロイで `GET /api/2fa/status` が **500** を返し、セキュリティ画面を開くたびにサーバー障害が記録されていた（当該実行で4回＝2ページ表示×react-query の `retry: 1`）。`TwoFactorAuthService` がシークレット無しで `RuntimeError` を送出するのは**意図的な fail-closed 設計で正しい**（共有既定鍵で TOTP シードを暗号化しない）。誤っていたのは**報告方法**で、`/api/2fa/*` の6ハンドラすべてが素の `except Exception` で捕らえ 500 を返し、モジュール未提供時は 404 を返す — **同一事象に3種のコード**が混在していた。HTTP セマンティクス上、**500 は「予期しない状況」＝バグ**、**501 は「サーバーがその機能をサポートしていない」**、503 は一時的障害。「このデプロイに2FAが無い」は既知の能力状態であり 501 が正しい。500 で報告し続けると #10/#12 の Prometheus 5xx 計装とエラーバジェットが恒常的に汚染され**本物の障害が埋もれる**。修正: `_two_factor_available()`（モジュール import 済み**かつ**サービス構築可能かを実際に試す。`_wire_auth_2fa()` と同様**意図的に非キャッシュ**とし、後から env を設定すれば再起動なしで有効になる）を追加。`GET /api/2fa/status` は未構成でも **200 + `available:false`**（「自分の2FAは有効か」への正answerは「無効、かつここでは提供されない」であってエラーではない）。`setup/enable/verify/verify-backup/disable` は **501**＋明示メッセージ（404分岐も統合）。`/ready` に `two_factor` を optional として追加し未構成を **degraded** で可視化 — ただし**名前チェックでは不可**（2FA は旧来の一括 `try/except` 経由で読まれ `_SUBSYSTEM_ERRORS` に載らず、かつシークレット欠落は import ではなく構築時の失敗）なので probe 方式にした。フロントは「2FAは無効」と誤表示し、ユーザーが設定を押して初めてエラーが出る状態だったため、`利用不可` バッジ＋説明を表示し設定ボタンを隠す。**`/api/auth/login/verify-2fa` は意図的に不変**（500 ではなく 401 を返し、未構成なら到達に必要な `PendingTwoFactor` 自体が発行されない）。検証: 実サーバー2台＋Playwright で、未構成側は status 200/`available:false`・mutating 5本すべて **501**・`/ready` が **degraded** で `two_factor` を列挙、構成済み側は `available:true`・setup 完了・`ready`、そしてセキュリティ画面の **`/api/` 5xx がゼロ**（#51 で観測した 500×4 の解消を実証）。695バックエンド＋50フロントテスト緑、新規バックエンドテストは修正前に失敗することを確認 | `71cd388` |
| 52 | **フロントエンドの自動テストがゼロ（回帰防御がバックエンドに完全偏重）** | `frontend/package.json` にテスト依存もテストスクリプトも存在せず、20+ラウンドのフロント検証は**すべて使い捨ての Playwright スクリプト**（scratchpad）で行われリポジトリに1行も残っていなかった。結果、回帰防御は**バックエンド641件 vs フロント0件**に偏り、本ブランチで確立した不変条件（#35 の XSS 防御・#46 のトリアージ順・#47 の503文言・#44 の受け取り表示）を守るものが誰もいなかった。Testing Trophy（「Write tests. Not too many. Mostly integration.」/ Testing Library の指導原則「テストがソフトウェアの使われ方に似ているほど信頼できる」）に従い、**静的解析（既存 tsc/eslint）を土台に統合テストへ比重を置き、複雑な純粋ロジックのみ単体**とした。クエリはDOM構造ではなく**ロール・ラベル経由**にしたため、アクセシビリティの回帰防御も兼ねる。統合: `ParameterDelivery`（購入JSONの表示とクリップボードへの完全一致・有料/無料再取得の文言差・空状態＝#44）、`ForgotPassword`（**存在しないアドレスでも同一文言**＝列挙防止 #42、トークン非露出 #51、dev時のみ近道、失敗時に送信済みと言わない）、`VerifyEmail`（リンクのトークン事前入力・手動入力のtrim・成功画面・期限切れ表示 #51）、`StarRating`（radiogroup/radio・aria-checked・読み取り専用時はradioなし）。単体: `isSafeHttpUrl`（`javascript:`/`data:`/`vbscript:`/`file:` を拒否＝#35 の描画側）、`severityOf`（トリアージ順と未知reasonのフォールバック固定 #46）、`apiErrorMessage`（detail抽出と503 #47）、`useDebouncedValue`。**フットプリント最小化を意図的に設計**: テスト関数を `vitest` から明示 import し globals を使わず、jest-dom は `src/test/setup.ts` で登録 → **`.eslintrc.json` も `tsconfig.json` も変更不要**で `npm run build`/`npm run lint`（`--max-warnings 0`）がクリーンのまま。テストは `src/` に同居させ `tsc --noEmit` の型検査対象にした（Trophy の土台）。globals を切ったため Testing Library の自動 cleanup が働かない点は setup で明示呼び出し。**ミューテーション検証**: `isSafeHttpUrl` を常に true 化／`REASON_SEVERITY` の malware を降格／クリップボードへ別ペイロード送信、の3改変で**該当3領域の10テストが失敗**し、復元で全緑に戻ることを確認。計47テスト/8ファイル、バックエンド641件は不変 | `b2f1e49` |
| 51 | **メール配送が存在せず、①本番でパスワード復旧不能 ②メール確認が自己申告（実バグ）** | `main/` 全体に smtplib/sendgrid 等のメールコードが**一切なく**、2つの穴が同根で存在した。①`POST /api/auth/password-reset` はトークンを生成するが配送手段がなく、**本番でパスワードを忘れたユーザーは恒久的にロックアウト**（開発用 `COCOA_EXPOSE_RESET_TOKEN` だけが唯一の経路）。②`register()` と `resend-verification` は `email_verification_token` を**応答ボディで返しており**、メール確認の目的（メールボックス所有の証明）が成立しない — **他人のメールアドレスで登録して自分で確認済みにできた**。OWASP の forgot-password ガイダンスは「統一応答・単回使用トークン・**サイドチャネル配送**」の3点を要求し、Cocoa は前2つ実装済み・配送だけ欠落という状態だった。新規 `main/email_sender.py`（`ConsoleEmailSender`=開発既定・ログ行が配送そのもの、`SMTPEmailSender`=stdlib、`COCOA_SMTP_*` で設定、`send_email()` は request path に例外を漏らさない best-effort）。reset/register/resend の3経路にメール送信を配線（`COCOA_PUBLIC_URL` 基点のリンク）。**応答内トークンは `COCOA_EXPOSE_VERIFY_TOKEN`（既定 OFF）にゲート** — 既存の RESET 用と同じ opt-in 慣行。フロントは新規 `/verify-email` 着地ページ（`?token=` 自動入力・ログイン不要 = リンクはどの端末でも機能する必要がある）、Security に未確認バナー＋再送、Register 成功時に確認メール案内。テスト15件（sender 8 + エンドポイント契約 7、**契約7件は旧コードで全て失敗**）。E2E は本番姿勢サーバー（露出なし）と開発サーバーの2台で実施: 前者は register/resend/reset の応答に**トークンが一切含まれない**こと、後者は登録→未確認バナー→再送→確認→バナー消失＋ #42 回帰＋**サーバーログに3通のコンソールメール（正しいリンク付き）**を確認。`/api/` 4xx/5xxゼロ、365テスト緑、build/lintクリーン。SMTP プロバイダ選定（SaaS 契約）は事業判断のため対象外 | `df758c6` |
| 50 | **取り下げが1出品限りで、常習出品者に対処できない（出品者の履歴が見えない）** | 取り下げは1つの出品を消すだけで、違反を続ける出品者は publish し直せば済む。しかもモデレーターにはそれが**見えない**: 通報は `listing_id` しか持たず、UI は各通報を独立に裁定していた。エンフォースメントの技術情報は「常習犯は**出品者の時系列の記録**を見て初めて可視化される」「対応は出品単位からアカウント単位へ**段階的にエスカレート**すべき」と一貫して述べている。さらにこの記録は**クライアント側で組み立てられない** — 取り下げ済みの出品は公開読み取りできず、まさにモデレーターが所有者を知りたい場面で 404 になるため、`get_reports()` がサーバー側で解決するようにした。各通報に `listing_name`・`listing_is_active`・`owner_id`・`owner_username`・`owner_history {reports_total, upheld_total}` を付与。履歴は**現在のページではなく全通報**から算出（ページングで出品者の記録が変わらないため）。`upheld_total` は**モデレーターが是認した通報のみ**を数え、却下された通報は加算しない（競合が通報を乱発して違反履歴を捏造できないようにするため。#46 で通報件数を表示して対処した武器化リスクと同型）。フロントは通報が2件以上ある出品者の記録を各行に表示し、**是認2件以上**の出品者を強調して「アカウント単位の対応」を促し、理由必須の「出品者を停止」を提供。Playwright検証: 同一出品者の3出品への3通報のうち1件を是認・1件を却下すると `reports_total=3 / upheld_total=1` となり**却下は違反履歴にならない**。取り下げ済み出品の通報でも所有者が読み取れる。コンソールに記録が表示され停止を実行すると、**理由と実行者（#49 の修正が効いている）が記録**され、当該出品者の次回ログインが **401 で拒否**される。621＋229テスト緑、build/lintクリーン | `b5d8094` |
| 49 | **クリエイター認定申請の審査UIが皆無＋管理者操作の実行者が記録されない実バグ** | 申請側は配線済み（`POST /api/auth/creator-application`）だが**管理側の呼び出しが1件もなく**、申請は永久に `pending` のまま滞留し、認定バッジは製品上一度も付与できなかった（#46 と同じ「誰も開けないキュー」）。モデレーションコンソールに4つ目のタブを追加（申請理由・ポートフォリオリンク＝攻撃者入力のため `isSafeHttpUrl` 経由で描画・承認/却下・確認内容の必須入力）。**バッジの正直さ**: この審査で確認できるのは申請理由とポートフォリオのみで本人確認は含まれない。信頼バッジの技術情報は「バッジは**何を確認したか**を明示すべき。証拠を超える意味を持たせると、利用者はあらゆるシグナルを無視するようになる」と明言しており、タブに審査範囲を明示し、バッジの tooltip も「運営が申請理由とポートフォリオを確認済み（本人確認・法人確認は含みません）」に変更した。**検証中に実バグを発見**: `review_creator_application()`・`ban_user()`・`unban_user()` は実行者を `admin_payload["sub"]` から読むが、`get_current_admin()` が渡すのは `"user_id"` キーの正規化済み dict。既存テストは全て生JWT形状（`sub`）で書かれていたため**テスト形状と本番形状の乖離**（パッケージimport事故と同型）で発覚しなかった。本番では実行者IDが常に `""` となり、①**承認にもBANにも実行者が記録されない**（説明責任の欠落）、②`ban_user` の自己BAN防止が `user_id == ""` の比較となり**一度も発火せず、管理者が自分をBANして管理機能ごとロックアウトできる**状態だった。`AuthManager._actor_id()` で両形状を受け付けるよう修正し、本番形状のテスト4件を追加（3件は修正前に失敗、1件は生JWT互換を固定）。Playwright検証: 申請がポートフォリオリンクと審査範囲注記つきでキューに表示され、確認内容なしの承認は拒否、承認で `is_creator_verified` が true になり、**判断理由と実行者の両方が記録**され、公開プロフィールのバッジに範囲限定の tooltip が付く。`/api/` 4xx/5xxゼロ、616＋229テスト緑、build/lintクリーン | `6035382` |
| 48 | **出品後に編集できない（`updateListing` が未使用）＋通報後の編集が見えない** | `marketplaceService.updateListing()` は型定義まで揃っていたが**どのコンポーネントからも呼ばれておらず**、出品者は誤字・価格・説明を一切修正できなかった。変更するには出品を捨てて新規作成するしかなく、その場合レビュー・評価・ダウンロード履歴を全て失う。バックエンドの `update_listing()` は変更ごとに価格履歴とバージョンを記録する実装が完成しており、記録の器はあるのに**それを生む手段がなかった**。`MyListings` に「編集」パネルを追加（公開後に変更して安全な項目＝名前・説明・価格・タグ・サムネイル・ライセンス。価格変更時は「価格履歴に記録され購入者に公開される」旨を明示）。加えて、出品編集は**通報後の内容すり替え（bait-and-switch）**を可能にするため、モデレーションコンソールの通報行に `listing.updated_at > report.created_at` の場合「**通報後に編集あり**」バッジを表示。**編集ロックではなくシグナルにしたのは意図的**: 大手は審査中の出品を凍結するが、「未処理の通報があれば凍結」にすると競合が通報するだけで相手の出品を止められる（#46 で通報件数を表示して対処した武器化リスクと同型）。モデレーターに「目の前の内容は通報された内容と違うかもしれない」と伝えつつ、通報者にその力を与えない設計とした。Playwright検証: 編集で名前・説明が更新され、価格 40→25（`is_free=False` で保存、#44 の修正が有効）、タグも置換。価格変更が**価格履歴の2件目**として記録される。買い手が通報した後に出品者が再編集すると、管理者に「通報後に編集あり」が表示される。`/api/` 4xx/5xxゼロ、バックエンド変更なし（616テスト緑）、build/lintクリーン | `131bde6` |
| 47 | **依存モジュール障害が「空の200」になり障害が不可視（52エンドポイント）** | 信頼性設計の技術情報が挙げる代表的アンチパターン「依存が壊れているのに空リストを返すと、"本当に結果が0件" と区別がつかない」に該当する状態だった。原因は2つ: ①コアサブシステム19個が**単一の `try/except ImportError`** で一括インポートされており、1モジュールでも壊れると**全19名が同時に `None`** になる（しかもブロックには "always available — pure stdlib" とコメントされており、まさにその前提が破綻を見えなくしていた）。②その結果 52 のエンドポイントが `{"items": []}` を **HTTP 200** で返す一方、同一ファイル内の 139 の同種ガードは 503 を返しており、**同じ障害が場所によって正反対の応答**になっていた。障害時にマーケットプレイスが「空の店」として正常応答するため、ステータスコードにもログにも異常が出ない。修正: `_import_subsystem()` でモジュール単位に分離インポート（パッケージ形式優先＋フラット fallback、失敗理由を `_SUBSYSTEM_ERRORS` に記録）し、1つの破損が他18個を巻き添えにしないようにした。52件の「空の200」を 503 に統一（各サブシステムが既に使っている文言を再利用）。対象は全て marketplace/auth/cart/collections 等の**コア**サブシステムで、空応答が正当なオブザーバビリティ系は含まれない。`/ready` を **critical / degraded の2分類**に拡張: critical（auth・marketplace・cart）欠落は 503（ロードバランサから外す）、optional のみ欠落は 200 + `status="degraded"`（機能は減るが提供継続）とし、どのサブシステムがなぜ落ちたかをインポート失敗理由つきで返す。加えて `idem_key` を**文字列の場合のみ**構築するよう修正（FastAPI 経由では str|None だが、それ以外の値は truthy となり異なるエンドポイント同士が1つのキャッシュエントリで衝突しうる）。実アプリ検証: marketplace を落とすと `/ready` が 503 `not_ready`（`missing_critical: ['marketplace']`）、`GET /api/marketplace` が**空ページではなく 503＋明示メッセージ**を返す。wishlist のみ落とすと `/ready` は 200 `degraded` でマーケットプレイスは 200 で提供継続。616テスト緑、新規7件は修正前に失敗することを確認済み | `0881471` |
| 46 | **管理・モデレーションUIが皆無（46エンドポイントが到達不能）** | `/api/admin/*` は46エンドポイント（全224中21%）あるがフロントからの呼び出しが**1件もなく**、リポジトリ最大の「実装済み・到達不能」ブロックだった。通報の受付は買い手側に完全配線され、払い戻し申請も出せるのに、**それを裁定する手段が存在しない**ため、通報も返金申請も永久に `pending` のまま滞留する（curl で叩く以外に道がない）。信頼ループが「誰も開けないキュー」で終わっていた。`adminService.ts`（通報・レビュー通報・払い戻しの一覧/裁定/承認/却下＋復元）と `pages/admin/Moderation.tsx`（3タブ、ルート `/admin`、ナビは admin/moderator にのみ表示）を新設。**モデレーション運用の技術情報に基づく設計**: ①**重大度トリアージ** — 到着順ではなくリスク順（マルウェア・著作権を最優先）に並べ、同順位内は古い順。②**集約シグナル** — 同一出品への未処理通報件数を各行に表示（単一通報での即断は競合による通報の武器化を招くため）。③**判断理由の必須化** — 裁定には理由入力を必須とし、バックエンドが `resolution_note` と `resolved_by` として保存（次の担当者が経緯を再構築せずに済む）。④**キュー健全性** — 未処理件数と最古未処理の経過時間を表示。⑤**復元可能性** — 取り下げ済みの行に「取り下げを取り消す」を表示（#45 の復元エンドポイントを使用）。Playwright検証（`COCOA_ADMIN_PASSWORD` 付き実サーバー）: 非管理者はナビに項目が出ず `/admin` で拒否される／管理者は**後から通報されたマルウェアがスパムより上位**に表示される／理由未入力の取り下げは実行されない／理由付きで取り下げると公開検索から消え、通報に理由と裁定者が記録される／管理者復元で検索に復帰／払い戻し承認で**実際にクレジットが移動**（買い手 30→50）。`/api/` 4xx/5xxゼロ、バックエンド変更なし（609テスト緑）、build/lintクリーン | `6c0ed41` |
| 45 | **取り下げが取り消せない（再公開手段が存在しない）** | モデレーション設計の技術情報（通報キューは「単一通報での自動非表示を避け、復元可能性と監査ログを前提にする」ことを共通の要件として挙げる）を踏まえ、管理UIに着手する前に復元経路の有無を確認したところ、**リポジトリ全体で `is_active = True` を設定する箇所が1つも存在しない**ことが判明。`unpublish()` は論理削除（`is_active=False`）でリスティングID・レビュー・評価・ダウンロード履歴を保持する設計なのに戻す手段がなく、**出品者が一時的に取り下げると恒久的に失う**（新規出品し直す＝履歴全喪失以外に手段なし）。モデレーション側も同様で `resolve_report(takedown=True)`・`admin_deactivate()` に逆操作がなく、**誤った取り下げが売り手のリスティングを恒久破壊**する状態だった。`MarketplaceStore.republish()`（オーナー限定）と `admin_restore()`（所有者不問・冪等、`admin_deactivate` の対）を追加し、`POST /api/marketplace/{id}/republish`・`POST /api/admin/listings/{id}/restore` を新設。両者とも共有ヘルパ `_reindex_listing()` で検索索引に再登録する（`is_active` だけ戻すと「公開中を称しながら検索に出ない」不整合になるため）。復元時はオーナーに通知。`MyListings` は非公開のリスティングに「再公開」ボタンを表示。回帰テスト8件（オーナー/非オーナー・不明ID・冪等性・履歴保持）。Playwright検証: 出品→ダウンロード（履歴1件）→UIで取り下げ（公開検索から消える）→「再公開」ボタン出現→再公開で公開検索に復帰し、**listing_id と download_count が保持されている**ことを確認。`/api/` 4xx/5xxゼロ、609テスト緑、build/lintクリーン。**この修正は管理コンソール（3-5）の前提**: 取り下げをワンクリック化する前に復元経路を用意した | `77a1fd9` |
| 44 | **買い手が購入した商品を受け取れない**（＋価格を表示したまま無料譲渡される実バグ） | First Principles で「マーケットプレイスが不可欠に満たすループ」を白紙から導出して突き合わせた結果、**買い手ループの中心が欠落**していると判明。Cocoa の商品はアバターパラメータ（本文書1節の意図的スコープ）であり `POST /api/marketplace/{id}/download` が `avatar_data.parameters` として返すが、**フロントに呼び出しが1件も存在しなかった**。決済・注文・ライセンス発行・履歴記録は全て動くのに、買った本人が中身を取得する手段が UI 上に皆無（履歴ページは名前を並べるだけ）。`AvatarData` 型・`checkOwnership`/`downloadAvatar`（どちらも未接続だった）・新規 `ParameterDelivery.tsx`（整形JSON表示＋クリップボードコピー＋`.json`保存＋VRChatツール導線）を追加し、`ListingDetail` と `DownloadHistory` に配線。**取得ボタンは無料になる場合（出品者本人・無料作品・購入済み）のみ表示**し、有料未購入はカート経路のみとして誤課金の第2経路を作らない設計とした。**検証中に実バグを発見**: `is_free` は既定 True で `price_credits` と独立だったため、価格のみ指定して出品すると（Webフォームは両方送るが、APIクライアントは指定しないのが自然）`is_free=True` のまま保存され、`download()` の課金判定 `not listing.is_free` により**価格を表示したまま0クレジットで引き渡され、売り手の収益もゼロ**になっていた（再現: price_credits=30 → amount_paid=0、双方の残高不変）。`is_free` は価格の従属属性であるため `publish_avatar()` で導出し `update_listing()` で再導出するよう修正、回帰テスト7件を追加（うち5件は修正前に失敗することを確認済み）。Playwright検証: 価格のみ指定の出品が `is_free=False` で保存され、購入前は取得ボタンが出ず、カート購入で12cr減算、取得で**出品時と同一のJSON**が表示され、再取得しても残高不変、履歴からも同一JSONを取得。`/api/` 4xx/5xxゼロ、601テスト緑（3件は環境起因の既存エラーで変化なし）、build/lintクリーン | `02a53ad` |
| 43 | クリエイターのフォロワー/フォロー中一覧UI皆無 | `GET /api/users/{id}/followers`・`.../following`（公開: `public_profile()` の配列を返す）は完成・テスト済みだったがフロント呼び出しがゼロで、クリエイターページはフォロワー数を静的テキスト表示するのみ、閲覧者自身のフォロー一覧からフォローボタンの状態を導出するだけだった。誰がフォローしているか/誰をフォローしているかを閲覧する手段が皆無。`userService`に`getFollowers`/`getFollowing`（`{items,total}`）を追加、`Creator.tsx`のフォロワー数をクリック可能なトグルにし「フォロー中を見る」トグルを追加、双方が開いている間だけマウントされるオンデマンドの`FollowPanel`（アバター・表示名・認証バッジ・@ユーザー名の公開プロフィール行、各行は`/users/:id`へリンク）を展開。`components.css`にリンク風インラインボタン`.text-link-button`を追加。実uvicorn+Playwright検証: API経由でB→A・A→Cのフォローを構築し、Aのページでフォロワーパネルに B・フォロー中パネルに C が表示され、行クリックで当該クリエイターへ遷移。`/api/` 4xx/5xxゼロ、バックエンド変更なし、build/lintクリーン | `e067383` |
| 42 | パスワードリセット（パスワード忘れ）フローのUI皆無 | `POST /api/auth/password-reset`・`.../confirm` は完成・テスト済みだったがフロント未接続で、パスワードを忘れたユーザーがアカウントを回復する手段が皆無だった（本番マーケットプレイスとしての実質的欠落）。ログインページに「パスワードをお忘れですか？」リンク、新規 `/forgot-password`（メール入力→存在の有無を漏らさない一律応答＝バックエンドの列挙防止設計と一致。開発環境で `COCOA_EXPOSE_RESET_TOKEN=true` 時は dev_token とconfirmページへの直リンクを表示）、新規 `/reset-password`（メールリンクの `?token=` から自動入力・手動編集可＋新パスワード＋確認フィールドでクライアント側不一致チェック→成功時ログインへリダイレクト）。Playwright検証（`COCOA_EXPOSE_RESET_TOKEN=true` の実サーバー）: リンク→リセット申請で dev token 表示→トークン自動入力で進む→新パスワード設定→ログインへリダイレクト、その後**新パスワードは認証成功（200）・旧パスワードは拒否（401）**でパスワードが実際に変更されたことを確認。バックエンド変更なし（225/225）、build/lintクリーン | `0433420` |
| 41 | ギフトカードの事前確認（lookup）UI皆無 | `GET /api/gift-cards/lookup`（公開: 金額・有効性を購入者情報なしで返す）は完成・テスト済みだったがフロント未接続で、交換者はコードの金額や使用済みかどうかを実際に交換を試みるまで知る手段がなかった。ギフトカードページの交換フォームに「確認」ボタンを追加し、交換前に金額・有効期限を表示（使用済み/無効/期限切れの場合は明確なメッセージ）。lookup で無効が確定した場合は「使用する」ボタンを無効化し、既知の無効コードを送信できないようにした。コード編集時は古いプレビューをクリア。Playwright検証: 新規作成した10クレジットカードをlookupすると「10 クレジット」表示→交換で残高加算、存在しないコードのlookupはエラートースト（想定される404）でクラッシュせず処理。バックエンド変更なし（48/48）、build/lintクリーン | `a5f6f19` |
| 40 | VRChatパフォーマンス分析がUI未接続＋Quest指定で必ず400になるバグ | `POST /api/tools/vrchat/performance` は完成・テスト済みだったがフロント未接続。実装中に**バグを発見**: エンドポイントが `Platform.Quest if body.platform.lower() == "quest" else Platform.PC` としていたが、`Platform` enum は `PC`/`ANDROID`/`IOS` のみで `Quest` メンバーが存在せず、`Platform.Quest` が AttributeError を投げるため**Quest指定のリクエストが必ず400**になっていた（PCパスは動作）。QuestはVRChatではAndroidベースで分析器も非PCにANDROID_LIMITSを適用するため、quest/android→`Platform.ANDROID`にマップ。回帰テスト `test_performance_endpoint_accepts_quest_platform` をパッケージimport回帰クラス（サブプロセス）に追加。`VRChatTools.tsx` をタブ化（パラメータ予算/パフォーマンスランク）、新規 `PerformanceTool`（ポリゴン/マテリアル/ボーン/PhysBones/テクスチャメモリ/PC・Quest入力→ランク・スコア・検出された問題・カテゴリ別改善提案を表示）。E2E中に発見したフロントクラッシュも修正: レスポンスの `issues` は文字列でなくオブジェクト配列（`{severity, category, message, current, target}`）で、直接描画するとReact error #31でErrorBoundaryに落ちていた→型修正し`issue.message`を描画。Playwright検証: Questパス（旧400）が「Very Poor」＋ポリゴン数問題＋Polygon Reduction/Material Optimization提案を返す、PCパスも描画。4xx/5xxゼロ、バックエンド114/114（新規1件）、build/lintクリーン | `5000049` |
| 39 | VRChatパラメータ予算ツールがUI未接続＋本番で常に503になる import バグ | `POST /api/tools/vrchat/budget`（256ビット予算分析・最適化提案）と `/performance` は完成・テスト済みだったがフロント未接続。実装中に**本番バグを発見**: 両エンドポイントが `from vrchat_parameter_budget import ...` というフラット import を使用していたが、正規の起動方法 `uvicorn main.api_server:app`（Docker CMD・本番）ではこのファイルが `main` パッケージ配下にあるため bare import が ImportError となり、両ツールが**常に503を返す**状態だった。他のsibling importと同じ「相対import優先→フラットフォールバック」に修正。回帰テスト `TestVRChatToolsPackagedImport` はサブプロセスで `import main.api_server`（失敗していた正確な文脈）を実行して検証（他のテストは意図的に main/ を sys.path に載せるため bare import が解決してバグを隠す）。修正前のフラットimportに対してテストが FAIL することを確認済み。新規公開ページ `/tools/vrchat`（トップナビ「ツール」）: パラメータ表（名前/型/同期）の追加削除→予算エンドポイント呼び出し→使用ビット/256ビット・プログレスバー・型別内訳・予算超過警告・最適化提案を表示。Playwright検証: Bool+Intで9/256ビット予算内、同期Float34個追加で予算超過警告（281/256, +25）とOSCビットパッキング提案。修正後4xx/5xxゼロ（503は修正前のみ）。バックエンド593/593（新規2件）、build/lintクリーン | `fbd82eb` |
| 38 | リスティングのバージョン履歴UI・評価分布の可視化が皆無 | `GET /api/marketplace/{id}/versions`（全バージョン履歴）・`POST .../versions`（新バージョン公開）・`GET .../rating-distribution`（星別分布）は完成・テスト済みだったがフロント呼び出しゼロ。詳細ページに評価分布セクション（5→1星のバーチャート、平均・件数、公開可視の投票のみ集計しヘッドラインの平均と一致、評価ゼロ時は非表示）とバージョン履歴セクション（新しい順、changelog 表示、オーナーには changelog + 任意の更新パラメータ（submit 前に JSON 検証）を受け取る公開フォーム、非オーナーには自動生成の v1 のみの場合は非表示）を追加。Playwright検証: 5★+4★のレビューで平均4.5と正しい比率のバー表示、オーナーが v2 を changelog 付きで公開→履歴の v1 の上に表示、非オーナーは同じ履歴を見られるが公開ボタンは非表示。バックエンド変更なし（591/591）、build/lintクリーン | `6851a22` |
| 37 | CC BY/CC BY-SAリスティングのクローン（リミックス）UI皆無＋ダウンロード履歴ページなし | `clone_listing()`（メタデータ＋パラメータの完全値をコピー、CC BY/CC BY-SAのみ許可・事前購入不要）は完成・テスト済みだったが呼び出しゼロで、プラットフォーム自体がリミックス文化を前提にしたライセンス選択肢を用意しているのに実際にリミックスする手段が皆無だった。`ListingDetail.tsx`に「クローンして自分の作品として公開」ボタン（`listing.license_type`が`cc_by`/`cc_by_sa`の場合のみ表示、`CLONEABLE_LICENSES`をサービス層でエクスポートしゲート条件がバックエンドの許可値と乖離しないようにした。オーナー自身でもクローン可、バックエンド側にも所有者制限がないため一致）。新規`me/DownloadHistory.tsx`（`GET /api/marketplace/downloads/history`、削除済みリスティングは「削除されたリスティング」と表示）。Playwright検証: CC BYリスティングにのみボタン表示（personal ライセンスでは非表示）、クローン実行で「...(clone)」という名前の新規リスティングが自分の所有として作成されCC BYを継承（さらにリミックス可能）、ダウンロード履歴に元のダウンロードが表示。4xx/5xxゼロ、バックエンド変更なし（591/591）、build/lintクリーン | `ed8863a` |
| 36 | フォロー中クリエイターのフィード・公開コレクション閲覧が皆無 | `GET /api/auth/feed`と`GET /api/collections/public`は完成済みだったがフロント未接続。フォロー機能自体はラウンド3で配線済みだったが、フォローする実利（新着通知以外の可視的な効果）が皆無だった。新規`me/Feed.tsx`（トップナビ「フィード」、マーケットプレイス/バンドルの隣）でフォロー中クリエイターの新着を新しい順に表示。`Collections.tsx`に「自分のコレクション/公開コレクションを探す」タブを追加、公開タブはデバウンス検索＋`Collection.to_dict()`が`owner_id`のみで`owner_username`を持たないため`getPublicProfile()`によるオンデマンド解決（チップ送信履歴・ライセンス一覧で確立した同一パターンを再利用）。Playwright検証: フォロー中クリエイターが新規公開した直後にフィードへ即座に反映、公開コレクションがオーナー名・説明付きで表示、一致しない検索語で正しく空状態にフォールバック。4xx/5xxゼロ、バックエンド変更なし（390/390）、build/lintクリーン | `0320fd5` |
| 35 | リスティング譲渡・在庫上限設定のUI皆無（ユーザー名検索エンドポイントも未接続） | `transfer_listing()`・`set_stock_limit()`は完成・テスト済みだったがフロント呼び出しゼロで、クリエイターはリスティングを別アカウントに移す・販売数に上限を設ける手段がなかった。譲渡APIは`new_owner_id`（生のユーザーID）を要求するがそれを知る手段がなかったため、`GET /api/users/search`（これも完全に未接続だった）を使ったユーザー名検索オートコンプリートを新設。`MyListings.tsx`の各行に「詳細設定」トグル（デフォルト折りたたみ、ライセンス/取り下げボタンで既に混雑していたため）→在庫（現在の状態表示＋上限設定＋「無制限にする」）と譲渡（ユーザー名検索→選択→`window.confirm`で不可逆であることを明示した上で確定）。Playwright検証: 在庫上限5設定で「残り5/5」表示→無制限化で「無制限」に復帰、ユーザー名検索で候補表示→選択→譲渡確定→元のオーナーの一覧から消失・`GET /api/marketplace/mine`で新オーナーの所有をAPI側からも確認。4xx/5xxゼロ、バックエンド変更なし（816/816）、build/lintクリーン | `625b2ef` |
| 34 | リスティング/レビューの通報UIが皆無（信頼性・安全性の穴） | `POST /api/marketplace/{id}/report`と`reviews/{id}/report`は理由enum・自己通報/重複審査中の拒否・統合モデレーションキュー連携まで完成済みだったがフロント呼び出しがゼロで、買い手が不適切なコンテンツを報告する手段が一切なかった（既存の通報レコードがあったとしても生API呼び出し経由のみで作られたことになる）。`ListingDetail.tsx`に「このリスティングを通報する」リンク（自分のリスティングには非表示）＋理由選択＋任意詳細。各レビューの「役に立った」の隣に小さな「通報」リンク（`ReportReviewButton`コンポーネント）。実装中に判明: レビュー行の「通報」ボタンとリスティング側「...を通報する」リンクは両方とも部分文字列「通報」を含むため、Playwrightの`has-text`セレクタが曖昧に一致（UIバグではなくテストスクリプト側の問題、`.row-list`スコープで解消）。Playwright検証: リスティング通報→確認トースト、レビュー通報→確認トースト、同一リスティングへの2件目の審査中通報が正しく400（「すでに...審査中です」）でクラッシュせず処理される。4xx/5xx想定内（重複通報の400のみ）、バックエンド変更なし（591/591）、build/lintクリーン | `2dfe6a7` |
| 33 | クリエイター統計・収益ダッシュボード皆無＋クリエイター認定申請への導線なし | `GET /api/marketplace/analytics/me`（タグ/カテゴリ別DL内訳・人気リスティング・評価分布）と`earnings/me`（販売/チップ別収益）は完成済みだったがフロント呼び出しゼロで、クリエイターは公開ストアフロントの基本3統計以上の情報を見る手段がなかった。また`Creator.tsx`はラウンド3から`is_creator_verified`の「✓ 認証済み」バッジを表示していたが、実際に申請する導線（`POST /api/auth/creator-application`、完成・テスト済みだが未接続）が存在しなかった。新規`me/CreatorDashboard.tsx`（「ダッシュボード」タブ）に3セクション: 統計（公開数/総DL数/累計売上/レビュー数＋人気リスティング・タグ・カテゴリ別内訳、既存のスタットタイル/リストパターンを再利用しチャートライブラリ導入を回避）、収益（7/30/90日切替）、クリエイター認定申請（理由＋任意ポートフォリオURL、審査中/認定済み/却下のステータス表示、却下時のみ再申請可）。`getMyCreatorApplication()`は未申請時の期待される404を「申請なし」として扱う（他のオプショナルリソース取得と同じパターン）。Playwright検証: 1件販売済みのクリエイターで統計が正確に反映（DL数1・売上20cr・人気リスティング表示）、申請→審査中→リロード後も状態保持・フォーム非表示を確認。4xx/5xxゼロ（捕捉された唯一の404は申請前チェックの想定内応答）、バックエンド変更なし（816/816）、build/lintクリーン | `e34fe5b` |
| 32 | APIキー管理・会員ランク表示・価格変更履歴のUI未接続 | 発見性ワークフローの`deferred_no_action`（優先度は低いが「良いフィラー」と評価済み）から3件。`APIキー`（`/api/auth/api-keys` CRUD）: 生キーは作成時に一度だけ返る設計に合わせ、`Security.tsx`に2FAのバックアップコード表示と同じ「一度だけ表示」パターンで追加、以降は`key_prefix`のみ表示。`会員ランク`（`GET /api/membership`）: `Profile.tsx`に読み取り専用カード（ランク名・手数料割引率・次のランクまでの必要クレジット）。`価格変更履歴`（`GET /api/marketplace/{id}/price-history`）: `ListingDetail.tsx`に折りたたみセクション、価格が実際に変動した（履歴2件以上）リスティングのみ表示し初回公開のみのリスティングに冗長な1行を出さないようにした。Playwright検証: 価格を2回変更したリスティングで新しい順に3件表示、新規アカウントのプロフィールで「ブロンズ・次のランクまであと1,000cr」表示、APIキー作成で`cca_...`が一度だけバナー表示され以降はプレフィックスのみ、無効化で一覧が空に。4xx/5xxゼロ、バックエンド変更なし（336/336）、build/lintクリーン | `a94f722` |
| 31 | コミッション（受注制作）UI皆無 — 発見性ワークフロー最終項目 | 発見性ワークフロー総合ランキングの最終項目。自身のランキングで「本セッション最大の残存範囲（2つのダッシュボード・6段階以上の状態遷移）」と明示的にフラグされ、無理に早いラウンドに詰め込まず独立ラウンドとして先送りされていた。状態遷移（pending→accepted/declined→delivered→closed、＋モデレーションキュー連携の紛争提起）は`main/commissions.py`で完成・E2E相当のテストまで揃っていたがフロント呼び出しがゼロだった。`Creator.tsx`にチップ/フォローの隣に「コミッションを依頼する」ボタン（タイトル・依頼内容・参考予算＝実際の支払いはマーケットプレイス経由で別途、とバックエンドのdocstring通りに明記）。新規`me/Commissions.tsx`（受け取った依頼/自分の依頼タブ）は閲覧者の役割と現在のステータスに応じて有効なアクションのみ表示: クリエイターはpendingで承認/辞退・acceptedで納品、依頼者はpendingで取り消し・deliveredで受領クローズ、進行中/納品済みの双方で「問題を報告」（管理者モデレーションキューへ、`kind=\"commission_dispute\"`はサーバー側で宣言済みだったがこのUIが無ければ到達不能だった）。Playwright検証: 依頼→受け取った依頼タブで「承認待ち」→承認→「進行中」→納品→「納品済み」→自分の依頼タブに納品メッセージ表示→受領クローズ→双方で「完了」、全遷移で4xx/5xxゼロ。バックエンド回帰1327/1327（バックエンド変更なし）、build/lintクリーン。これでラウンド5〜9にわたる「バックエンド完成・フロント未接続」機能群（ライセンス・チップ・払い戻し・バンドル・コミッション）を完了 | `3489885` |
| 30 | バンドル（セット販売）UI皆無＋一時停止エンドポイント未配線 | 発見性ワークフロー総合ランキング4位、本セッション最大規模のラウンド。バンドル作成・購入APIは完成・テスト済みだったがフロントUIが皆無。実装中に発見: `BundleManager.deactivate_bundle()`/`activate_bundle()`はストア層で完成・単体テスト済みだったが、呼び出すHTTPエンドポイントが一つも存在せず、クリエイターが販売を止める唯一の手段が完全削除のみだった（`list_my_bundles`の`include_inactive`フラグも常に空振り）。`POST /api/bundles/{id}/activate`・`/deactivate`を新設（既存の作成/更新/削除エンドポイントと同じオーナー確認→403/404パターン）、テスト4件追加。公開ブラウズページ`Bundles.tsx`（トップナビ「バンドル」）: `Bundle.to_dict()`は`listing_ids`のみでリッチな商品情報や計算済み価格を持たないため、各カードが`ListingDetail.tsx`と同一の`["listing", id]`クエリキーで構成リスティングを解決しキャッシュ共有、サーバー側と同じ計算式（`unit_price * (100 - discount) // 100`）で割引後価格をクライアント側算出。クリエイター管理`me/Bundles.tsx`（「バンドル管理」タブ）: 自分の公開中リスティングからチェックボックスで2件以上選択→作成、一時停止/再開/削除。Playwright検証: 2点選択・25%引きでバンドル作成→公開中バッジ→購入者側で40cr→30cr表示を確認→購入で残高が正確に-30cr→クリエイターが一時停止→バッジが「一時停止中」に変化し公開一覧から即座に消失。4xx/5xxゼロ、バックエンド回帰1327/1327（新規4件含む）、build/lintクリーン | `a8cb891` |
| 29 | 払い戻し申請UI皆無（購入者の自己解決手段がなかった） | 発見性ワークフロー総合ランキング3位。`POST /api/refunds`（購入後72時間以内・1注文につき1件のpending/approvedリクエストのみをサーバー側で強制）と`GET /api/refunds/mine`は完成・テスト済みだったがフロント呼び出しがゼロで、購入者が問題のある注文に対して自己解決する手段がなく運営への手動対応に頼るしかなかった。`OrderDetail.tsx`の完了済み注文に「払い戻しを申請する」ボタン→理由入力フォーム、既存リクエストがあれば（`my-refunds`一覧を`order_id`で照合）フォームの代わりにステータス（審査中/承認済み/却下）と運営コメントを表示し、バックエンドが拒否するだけの重複申請導線を作らないようにした。新規`me/Refunds.tsx`（全申請履歴、ステータスバッジ、注文へのリンク）。Playwright検証: 購入→注文詳細→理由入力→申請→即座に「審査中」表示→`me/refunds`に同一リクエスト表示→再訪問時はフォームでなくステータスカード表示。4xx/5xxゼロ、バックエンド変更なし（160/160）、build/lintクリーン | `e59ba09` |
| 28 | チップ機能がUI未接続（送信・受信履歴とも皆無） | 発見性ワークフロー総合ランキング2位。`POST /api/tips`（冪等性キー対応・自己送金や1万クレジット超を拒否済み）と受信/送信履歴エンドポイントは完成・テスト済みだったがフロント呼び出しがゼロだった。`Creator.tsx`にチップ送信フォーム（フォローボタンの隣、金額+任意メッセージ、チェックアウトと同じ`newIdempotencyKey()`パターン）、新規`me/Tips.tsx`（受け取った/送った タブ）を追加。実装中に判明: `Tip.to_dict()`は`recipient_id`のみ保持し`recipient_username`を持たないため、「送った」タブでは初めて配線した`GET /api/users/{id}/profile`（`userService.getPublicProfile`）でオンデマンド解決。E2Eテスト作成中に「送る」ボタンのテキストセレクタが「チップを送る」トグルボタンにも部分一致し、フォームが送信されずに閉じるだけという事象が発生（UIバグではなくテストスクリプトの曖昧セレクタ問題と判明）→ 提出ボタンに`id="tip-submit"`を付与し解消。Playwright検証: チップ送信→トースト確認→送信者の「送った」タブにリンク付きで表示→受信者の「受け取った」タブに送信者名・メッセージ表示→残高が正確に+25。4xx/5xxゼロ、バックエンド変更なし（587/587）、build/lintクリーン | `4ab4da4` |
| 27 | ライセンスキー管理UI皆無（購入者は何を買ったか確認手段がなかった） | 発見性ワークフローの総合ランキングで最優先とされた項目: 有料・無料を問わず購入完了（直接ダウンロード・カートチェックアウト・バンドル購入の全経路）で`license_manager.issue_on_download()`がライセンスキーを自動発行済みだったが、購入者側に確認・アクティベートするUIが、クリエイター側に発行済みキーを確認・失効させるUIが、共に皆無だった。新規 `me/Licenses.tsx`（自分のキー一覧、リスティング名リンクは`ListingDetail.tsx`と同一クエリキーでキャッシュ共有、アクティベーション履歴、用途メモ付きアクティベートフォーム、上限到達時は無効化）。新規 `me/ListingLicenses.tsx`（`me/MyListings.tsx`の各行から遷移、オーナー専用、保有者ID・アクティベーション回数・失効操作）。実装中に判明: リスティング名自体に「ライセンス」という語を含めるとPlaywrightの`has-text`セレクタがリスティング名リンクと機能ボタンを曖昧に一致させることがあり、`aria-label`を追加して解消（副次的にアクセシビリティも向上）。Playwright検証: 購入→マイライセンスに表示（有効バッジ）→メモ付きアクティベート→履歴に記録、クリエイター側の同一キー確認→失効で「失効済み」に変化。4xx/5xxゼロ、バックエンド変更なし（631/631）、build/lintクリーン | `a2be559` |
| 26 | 保存済みXSS（プロフィールURL）＋プロモコードの譲渡未反映・クォータ誤集計（ワークフローによる敵対的検証済み監査で発見） | 並列調査ワークフロー（バックエンドギャップ探索・セキュリティレビュー・正しさレビュー・品質レビューの4系統＋各所見への敵対的検証）で発見・確認した3件。**保存型XSS（高深刻度）**: `auth_manager.update_profile()`が`website_url`/`avatar_url`のスキームを検証しておらず、ラウンド3で追加した`Creator.tsx`が両者を生の`&lt;a href&gt;`/`&lt;img src&gt;`として公開・未認証ページに描画していたため、`javascript:`URIを保存すればクリックした任意の訪問者（管理者含む、JWTはlocalStorageに常駐）のブラウザで実行される状態だった。`_sanitize_public_url()`（http(s)のみ許可、それ以外は例外を投げず黙って空文字列に）を新設し両フィールドに適用、`Creator.tsx`にもクライアント側の`isSafeHttpUrl()`を防御的に追加。**注意**: `social_links`の値は`@alice`等のハンドルでありURLではないため、同じサニタイザを適用したところ既存テストが失敗して発覚・除外した（過剰適用の自己訂正）。**プロモコードの譲渡未反映**: `transfer_listing()`がリスティングの`owner_id`のみ更新し`PromoCode.creator_id`を放置していたため、リスティング固有の有効なコードが譲渡直後に解決不能になり、旧オーナーは無効化可能・新オーナーは操作不能という状態だった。譲渡時に該当コードの`creator_id`も再割当（`listing_id=None`の汎用コードは意図通り元オーナーに残す）。**クォータの誤集計**: `create_promo_code()`の上限（50件）が無効化済みコードも含めた生涯合計を数えていたため、季節プロモ等で作成・無効化を繰り返すクリエイターが実質0件所有でも永久にロックされる状態だった。有効なコードのみでカウントするよう修正。回帰テスト10件追加。バックエンド699/699・225/225、build/lintクリーン | `0990d13` |
| 25 | プロモコードのパイプラインが両端未接続＋通知ミュート設定UI皆無 | カートは`promo_code`送信済み・チェックアウトは割引適用済みだったが、**作成側（クリエイター）も入力側（購入者）もUIが存在せず**パイプラインの両端が空だった。新規 `me/PromoCodes.tsx`（作成フォーム＋一覧＋ステータスバッジ「有効/期限切れ/使用上限到達/無効化済み」＋無効化）。`Cart.tsx` にコード入力＋`lookup_promo_code`での事前検証（無効コードをチェックアウト時の無言no-discountにしない）＋取り消し線付き割引表示、リロード後も`item.promo_code`から`useEffect`でプレビュー再構築。通知ミュート設定（`GET/PUT /api/notifications/preferences`）を通知ページに折りたたみセクションとして追加。Playwright検証: 作成→カートで適用（20cr→10cr表示）→チェックアウトで実際に10cr課金（残高50→40）を確認、別セッションでコード保存済みカートをリロードしても割引表示が復元、ミュートトグルが`muted_kinds`に反映。4xx/5xxゼロ、バックエンド変更なし（691/691）、build/lintクリーン | `f1e4cb4` |
| 24 | クリエイターページ・フォロー・トレンドがUI未接続（フォロー通知が発火不能） | フォロー系はサーバー側で完結済み（follow/unfollow・フォロワー数・`publish_avatar` のフォロワー通知配信）だったが、呼ぶUIが皆無のため実ユーザーには通知経路が永久に発火しない状態だった。ストアフロント（`GET /api/users/{id}/storefront`）・トレンド（`GET /api/marketplace/trending`）も同様に未接続。新規 `/users/:userId` ページ（プロフィールヘッダ＋認証済みバッジ＋フォロワー数＋フォロー/解除ボタン＋統計タイル＋リスティンググリッド）、詳細ページの「by クリエイター名」をリンク化（クリエイターが初めて回遊可能に）、`me/Following` タブ（一覧＋ワンクリック解除）、ランディングのトレンドストリップ（検索/フィルタ非適用時のみ表示＝検索結果を押し下げない）。Playwright検証: トレンド3件表示・詳細→ストアフロント遷移・フォローでボタンが「フォロー中」/フォロワー1人に変化・following一覧→解除で空に。4xx/5xxゼロ、バックエンド変更なし（801/801）、build/lintクリーン | `f1bd2e5` |
| 23 | 紹介プログラムが実質機能不全（登録経路が未配線）＋レビュー返信UI皆無 | バックエンド突合調査で発見: `ReferralManager.apply_referral_code()` は完成・単体テスト済みで、成立側（`on_first_purchase` → 紹介元へ50cr）も3つの購入経路すべてに配線済みだったが、**呼び出す側が存在しなかった**。`POST /api/auth/register` に `referral_code` フィールドが無く、実際のサインアップから紹介レコードが作られる経路がゼロ＝ボーナスは永久に発火しない（`is_active`未シリアライズ・2FA有効化と同種の「配線漏れ」実バグ）。`RegisterRequest` に `referral_code: Optional[str]` を追加し、登録成功後に best-effort で適用（無効コード・自己紹介・マネージャ不在でも登録は失敗しない、signup bonus と同一パターン）。回帰テスト4件追加。UI: `Register.tsx` に招待コード欄（`/register?ref=CODE` から自動入力・編集可）、新規 `me/Referrals.tsx`（自分のコード＋招待リンクのコピー、統計タイル、招待履歴と成立バッジ）、「友達を招待」タブ。レビュー返信: バックエンド完備（`/api/marketplace/reviews/{id}/replies` POST/GET/DELETE）だがUI皆無だった。`ReviewReplies` コンポーネントをレビュー欄に追加（スレッド表示・インライン返信フォーム・自分の返信の削除、GET が要認証のため未ログイン時はクエリ自体を発行しない）。実uvicorn+Playwrightで検証: `?ref=` 付き登録（欄に自動入力）→紹介pending→有料リスティング購入→紹介元statsがconverted/+50crに変化→Referralsページに「購入済み」バッジと+50cr表示、返信の投稿→スレッド表示→削除。全フロー4xx/5xxゼロ。バックエンド1215/1215 | `662545c` |
| 22 | 発見性UI・AI生成開示・パラメータプレビュー・アクセシビリティ/性能（最新論文・業界情報リサーチに基づく） | 競合ベンチ（VRChat「試着」・手数料透明性）、EC検索ベストプラクティス（検索利用者はCVR2.5倍・オートコンプリートで摩擦30-50%減）、WCAG 2.2新設SC、EU AI Act第50条（2026-08-02施行）を調査。バックエンドに**完成済みだがフロント未使用**のAPIが複数存在していた。①検索オートコンプリート（`GET /api/search/suggest`、250msデバウンス）②カテゴリ/タグフィルタ（`/api/marketplace/categories`＋既存browse）③保存検索UI（`/api/search/saved` CRUD＋通知トグル、新規`me/SavedSearches.tsx`）④関連アバター（`GET /api/marketplace/{id}/related`）を配線。加えて実在しなかった2機能を新設: ⑤`is_ai_generated`フラグ（default False・後方互換、publish/update/to_dict＋リクエストモデルに配線、出品フォームのチェックボックス＋カード/詳細の「AI生成」バッジ）⑥パラメータプレビュー（`to_dict()`に`parameter_count`＋`parameter_keys_preview`＝キー名先頭10件を追加、**値は購入まで秘匿**のまま形状のみ開示＝パラメータ版「試着」）。さらにa11y/性能: skip-link・アイコンボタンのaria-label・`aria-pressed`・最小ターゲットサイズ、ルート分割（React.lazy+Suspense、メインバンドル298KB→254KB）、サムネ`loading="lazy"`、`usePageTitle`。実uvicorn+Playwrightで検証: オートコンプリート「fo」→「fox」候補表示・選択で検索遷移、カテゴリフィルタ、保存→保存検索ページに表示・通知トグル、詳細ページのAIバッジ/14件パラメータプレビュー（10チップ+「+4」）/関連2件、全フローでAPI 4xx/5xxゼロ。バックエンド1211/1211。**未実装/保留は据え置き**（Stripe決済・出金・永続化＝監査3節、C2PA署名＝アップロード基盤不在、MLレコメンド＝規模的に過剰） | `7aaa0e5` |
| 21 | 「買えるが売れない」フロントエンドの欠落、ウィッシュリストUI皆無、`is_active`のシリアライズ漏れ | バックエンドは`POST /api/marketplace/publish`・`PATCH/DELETE /api/marketplace/{id}`・`GET /api/marketplace/mine`・ウィッシュリスト一式（`GET/PUT/DELETE /api/wishlist/*`、価格下落/在庫切れ状態付き）を完備していたが、対応する画面が一切無かった。`me/CreateListing.tsx`（`MarketplaceStore.publish()`の制約に合わせたバリデーション付き出品フォーム）、`me/MyListings.tsx`（`include_inactive=true`で公開中/取り下げ済みを併せて表示、取り下げ操作）、`me/Wishlist.tsx`（`with_status=true`で値下がり/在庫切れ/削除済みバッジ、カートへの直接追加）、`ListingDetail.tsx`にウィッシュリスト追加/削除トグルを追加。実装中に発見: `MarketplaceListing.to_dict()`が`is_active`を返しておらず、`get_user_listings_page(include_inactive=True)`が公開中/取り下げ済みを一覧で返す設計であるにもかかわらず呼び出し元がどちらか判別不能だった（`main/avatar_marketplace.py`で修正、回帰テスト追加）。実uvicorn+Playwrightで検証: 出品→一覧表示→取り下げ（ラベルが「非公開」に変化しボタンが消える）、ウィッシュリスト追加→専用ページに反映（値下がり/在庫切れバッジ表示）→カートへ直接追加、を確認。全フローでAPI 4xx/5xxゼロ | `14f54bf` |

---

## 3. 不足 — 未対応（優先度順）

いずれも「一行修正」ではなく製品・法務レベルの判断を要するため、実装せず所見として残す。

### 3-1. クレジット購入フロー（実通貨 → クレジット）が存在しない【優先度: 最高】

- **事実**: `main/billing_service.py` の Stripe 連携はサブスクリプション課金専用
  （`mode == "subscription"`）。全 webhook ハンドラ（`_WEBHOOK_HANDLERS` に登録された
  `_handle_subscription_update` / `_handle_checkout_completed` / `_handle_invoice_payment` 等）を
  確認したが、どれも `marketplace_store.credit()` を呼ばない。
- **`credit()` の全呼び出し元**（検証: `grep -rn "\.credit(" main/ | grep -v "def credit"`）は
  ギフトカード還元・紹介ボーナス・返金・サインアップボーナスの 4 系統のみで、
  実通貨からの入金経路がゼロ。
- **帰結**: サインアップボーナス 50 クレジットを使い切ったユーザーに追加入手手段が無く、
  有償マーケットプレイスが商売として成立しない。
- **必要な実装**: Stripe one-time payment のチェックアウト + webhook + 冪等性処理。
  `main/idempotency.py` が既存なので再利用できる。

### 3-2. クリエイター売上の現金化（クレジット → 実通貨）が存在しない【優先度: 高】

- **事実**: Stripe Connect・payout・transfer に類する機構が皆無
  （検証: `grep -rin "connect\|payout\|transfer\|cashout" main/billing_service.py` → 送金関連ヒットなし）。
- **帰結**: 3-1 と合わせ、クレジット経済は入口・出口とも実通貨と断絶した閉鎖系。
  クリエイターは売上をプラットフォーム内でしか使えない。
- **注意**: 資金移動業などの法規制判断を伴うため、技術実装より先にビジネス判断が必要。

### 3-3. アカウント削除時の金銭・契約データの扱いが未決定【優先度: 高（法的リスク）】

セクション 2 の #11 で、非金銭データ（カート・ウィッシュリスト・コレクション・保存検索・
通知・2FA）のカスケード削除とパスワード確認必須のセルフ削除 API は実装済み。

- **残る事実**: `_cascade_delete_user_data()`（`main/api_server.py`）は
  マーケットプレイス残高・取引台帳・コミッション・紹介コード・会員ティア・ライセンスキーを
  意図的に削除対象外としている。理由はコメントに明記: 会計監査・契約記録としての
  保持義務があり得るため、削除するかハード削除するか匿名化するかは技術判断ではなく
  会計・法務判断を要する。
- **必要な実装**: 上記データを「削除」「匿名化（ユーザーIDのみ不可逆ハッシュに置換等）」
  「一定期間後に自動削除」のいずれで扱うかの方針決定後、`_cascade_delete_user_data()` に
  追加する。

### 3-4. ビジネスデータの永続化が存在しない【優先度: 高】

- **事実**: マーケットプレイス・カート・ギフトカード・コミッション・モデレーション・
  会員ティア・ライセンス・紹介・2FA など、事実上すべてのビジネス状態がプロセス内
  Python dict（`threading.Lock` 保護のシングルトンストア）のみに存在する。
  PostgreSQL（`main/database_manager.py`）と Redis（`main/redis_cache_manager.py`）の
  基盤は存在するが、`main/api_server.py` からの実利用は各 2 箇所のみ
  （検証: `grep -c "get_database_service()\|get_cache_manager()" main/api_server.py`）。
- **帰結**: プロセス再起動（デプロイ・クラッシュ）で全データ消失。複数ワーカー・
  複数レプリカでは状態が分裂するため水平スケール不可。
- **注意**: 全ストアが同一のシングルトン + Lock パターンで書かれているため、
  ストアの下に永続化バックエンドを差す移行は機械的に進めやすい。
- **#74 で解消（オプトイン）**: `COCOA_STATE_DIR` 設定時、**15ストア全て**が
  `state.json` に往復保存され再起動を生き延びる（`state_codec` が `to_dict()` の
  非可逆性を回避し、生きたオブジェクトを正確に復元）。検索インデックスは復元後に再構築。
  **残る保留**: 複数ワーカー/レプリカ対応（＝真のDB移行。単一プロセス前提は #76 で
  **強制**され、違反時は黙って壊れる代わりに起動を拒否するようになった）と
  APIキーの永続化（長寿命credentialの取り扱い判断）。
- **#71 で部分解消（オプトイン）**: 最小の整合単位である**アカウント＋クレジット残高/台帳**は、
  `COCOA_STATE_DIR` 設定時に JSON スナップショット（起動時ロード／終了時＋30秒毎のアトミック保存、
  破損時は起動拒否の fail-closed、0600）で再起動を生き延びる。既定は従来どおり完全インメモリ。
  **残る保留**: リスティング・カート・レビュー等の全ストア移行（`to_dict()` の非可逆性解消＝
  データモデル凍結を伴う）、APIキーの永続化（長寿命credentialの取り扱い判断）、複数ワーカー対応。

### 3-6. ~~同種の「構成状態を500で報告」候補~~ → **#54 で解消（実態はより重大な認可欠落だった）**

当初は #53 と同じ「構成状態を500で報告」候補として記録したが、#54 で実サーバー検証したところ**本質は Broken Function Level Authorization（OWASP API5:2023）** — `GET /backups`・`GET /security/report`・`GET /metrics`(JSON) が `get_current_user` のみで一般ユーザーに運用情報を開示していた。3本を管理者専用にし、サブシステム不在時は 503 を返すよう修正済み（#54）。

### 3-7. ~~返金経路が2系統あり、片方は払い戻し不能~~ → **#77 で解消（事業判断ではなく不変条件の破れだった）**

- **事実（実測）**: `POST /api/marketplace/{id}/download` は有料出品に対し**その場で課金する**（買い手 550→430、売り手 +120 を確認）が、**注文（order）を作らない**。
  一方 `POST /api/refunds` は `order_id` を必須とするため、**この経路で購入したものは払い戻し申請ができない**（実測: `400 注文が見つかりません`）。
  その穴を埋めるために `POST /api/marketplace/{id}/dispute`（24時間以内の購入争議）と `GET/POST /api/admin/disputes` が実装済みだが、**買い手側・管理側とも UI 未配線**。
- **現状の実害はゼロ**: フロントエンドは**有料の初回購入を必ずカート→チェックアウト経由**にしており（`ListingDetail.tsx`: 直接取得は無料・所有済み・出品者のみに提示）、注文が必ず作られる。**UI 経由では払い戻し不能な購入は発生しない**。
- **残る問題**: API を直接叩くクライアント（サードパーティ連携等）は払い戻し不能な購入を作れる。
- **なぜ保留か**: 解消策は「争議UIを配線して第2の返金経路を作る」「直接ダウンロードでの課金を廃止しカートに一本化する」「争議機構ごと削除する」のいずれかで、**どれも金銭の取り扱い方針の決定**を伴う。#57（お気に入り）と同様に**重複機構を安易に露出させない**方針から、指示があるまで実装しない。

### 3-5. First Principles 分析で新たに判明した不足（#44 ラウンド、未着手）

3-1〜3-4 が事業判断待ちであるのに対し、以下は**事業判断を要さない実装可能な欠落**。

| 項目 | 内容 | 規模 |
|---|---|---|
| ~~**管理・モデレーション UI 皆無**~~ **→ #46 / #49 / #62 で解消進行中** | 通報裁定・レビュー通報・払い戻し承認は `#46`、クリエイター認定申請の審査は `#49` で配線済み（重大度トリアージ・判断理由の必須化・復元導線・実行者記録つき）。BAN／出品者履歴は `#50`、通報者への結果通知は `#61`、**ユーザー名簿・クレジット付与は `#62`** で配線済み。**モデレーションキューのうち他に居場所が無い `commission_dispute` は `#68` で配線済み**（他3種は専用タブと重複するため意図的に非表示）。**残りの未接続 admin エンドポイント**（会員ティア調整・監査エクスポート・ロール変更・クオータ設定等）は引き続き未到達 | 小（続きは別ラウンド） |
| ~~出品後に編集できない~~ **→ #48 で解消** | `MyListings` に編集パネルを追加。あわせて通報後の編集をモデレーターに可視化 | — |
| お気に入り機能が未露出 | `addFavorite`/`removeFavorite`/`listFavorites` の3関数が未使用。バックエンド機能ごと UI に無い | 小 |
| ~~**import 失敗時に 200＋空を返すエンドポイント群**~~ **→ #47 で解消** | 実測は52件（正しく503を返すのは139件）。モジュール単位の分離インポート＋52件の503統一＋`/ready` の critical/degraded 分類で解消済み | — |

---

## 4. 過剰 — 死んでいる / 余剰な機能

> **#59 で大部分を削除実行（-35,520 LOC / 117ファイル）**。イーロン・マスクのエンジニアリング・アルゴリズム第2ステップ「部品を削除せよ」を適用。`uvicorn main.api_server:app` の import 推移閉包で到達不能な **55 モジュール**（BCI・ブロックチェーン監査・NFT・メタバース・ARクラウド・エッジAI・音声クローン・動画解析・写真→アバター・`billing_service`・preset残骸・tkinter GUI 等）と、それらだけを検証していた **62 テストファイル**を削除。生存33モジュールは全て保持し、core-4スイート890件緑・実サーバー起動・`/ready` missing_critical空を確認。削除後にフルスイートの失敗集合は 3失敗/54エラー → 1失敗/5エラー（残りは pytest/_cffi_backend/psutil 未導入の環境要因のみ）に縮小。以下の記述は**削除前の状態**の記録として残す。
>
> **#60 で完了（削除ウェーブ2）**。#59 の閉包は tkinter ランチャー `main.py` も起点に含めていたため、ランチャーとそこからしか辿れない部品が10モジュール生き残っていた。**製品の唯一の実体 `uvicorn main.api_server:app` のみ**を起点に再計算し、残る到達不能10モジュール（`config`/`config_validator`/`dependency_injection`/`grafana_integration`/`i18n_manager`/`logging_config`/`logging_manager`/`main`〈ランチャー〉/`security_manager`/`vrchat_sdk_integration`）＋死テスト12＋`services/` スキャフォールド一式＋壊れたストラグラー `test_integration_2025.py` を削除。**これで `main/` は製品が実際に import する 33 モジュールと1対1になり、本節「死コード」は解消**（フルスイート失敗集合 0失敗/3エラー、残りは pytest/_cffi_backend 未導入の環境要因のみ）。

### 4-1. 到達不能モジュール 60/95（63%）

`main/` 配下 95 モジュールについて、2 つの実エントリポイント（`api_server.py` と
`main.py`）からの import 推移閉包を BFS で計算した結果、**60 モジュールがどちらからも
到達不能**。BCI（脳コンピュータ接続）・ブロックチェーン監査・NFT・メタバース統合・
エッジ AI・音声クローン・写真→アバター生成などの野心的 R&D 群がこれに含まれる。

> **訂正（#44 ラウンドの再実測）**: 上記は tkinter ランチャー `main.py` を含む閉包での数値。
> Docker が実際に起動するのは `uvicorn main.api_server:app`（`docker/Dockerfile`）のみであり、
> **サーバ単独の閉包では生存 32/95（34%）、`main/` の 26,831/52,717 行（51%）が閉包外**。
> さらに `config_validator`・`grafana_integration`・`i18n_manager`・`main` の4モジュールは
> tkinter ランチャーからのみ到達可能で、サーバ実行形態では死んでいる。
> 運用判断にはこのサーバ単独の数値のほうが実態に近い。

再検証用スクリプト（リポジトリルートで実行）:

```python
import re, glob, os
def local_imports(path):
    src = open(path, encoding="utf-8", errors="ignore").read()
    mods = set()
    for m in re.finditer(r"from \.(\w+) import", src): mods.add(m.group(1))
    for m in re.finditer(r"^\s*import (\w+)$", src, re.MULTILINE): mods.add(m.group(1))
    for m in re.finditer(r"from (\w+) import", src): mods.add(m.group(1))
    return mods
files = {os.path.basename(p)[:-3]: p for p in glob.glob("main/*.py")}
graph = {n: local_imports(p) & set(files) for n, p in files.items()}
def reach(root):
    seen, stack = {root}, [root]
    while stack:
        for d in graph.get(stack.pop(), set()):
            if d not in seen: seen.add(d); stack.append(d)
    return seen
dead = sorted(set(files) - reach("api_server") - reach("main"))
print(len(dead), dead)
```

注意: この BFS は静的 import のみを見る。`main.py` が **subprocess 経由で**起動する
GUI ツール（`avatar_preset_linker_gui.py` 等）や、`__main__` を持つ独立 CLI は
「到達不能」と出ても実際には使われうる（後述 5 節）。60 件の一括削除は不可。個別精査が必要。

### 4-2. 置換済みの残骸 3 ファイル【削除推奨・未実行】

以下の 3 ファイルは最も厳格な基準で「死んでいる」ことを確認済み:

- `main/preset_manager.py`（+ `tests/test_preset_manager.py`）
- `main/preset_diff_core.py`（+ `tests/test_preset_diff_core.py`）
- `main/preset_schema.py`（+ `tests/test_preset_schema.py`）

根拠:
1. **importer ゼロ**: `grep -rln "preset_manager\|preset_diff_core\|preset_schema" main/ scripts/ setup_*.py`
   で自分自身とテスト以外のヒットなし（動的 import も `importlib\|__import__` で確認済み）。
2. **`__main__` エントリポイント無し** = 独立 CLI としての用途も無い。
3. **機能は置換済み**: `preset_manager` の役割（名前付きパラメータセットの保存）は
   `main/database_manager.py` の `create_avatar_preset(user_id, name, parameters)` が
   `/api/avatars`（api_server.py 内）で**実際に稼働中**。しかも旧実装には `user_id` の概念も
   `threading.Lock` も無く、仮に API に配線すればクロスユーザー脆弱性を生む代物。

削除はエージェント実行環境の権限制約でブロックされたため未実行。次の 1 コマンドで完了する:

```bash
git rm main/preset_manager.py main/preset_diff_core.py main/preset_schema.py \
       tests/test_preset_manager.py tests/test_preset_diff_core.py tests/test_preset_schema.py
# tests/run_safe.py から "tests.test_preset_manager" / "tests.test_preset_schema" /
# "tests.test_preset_diff_core" の 3 行も削除すること
```

### 4-3. 死コードに対するテスト投資 812 関数 / 8,333 行

到達不能な 60 モジュールのうち 58 個にテストファイルが存在し、その合計は
**812 テスト関数・8,333 行 = テストスイート全体の約 32%**。CI 実行のたびに、
本番でどのユーザーも通れない経路を検証し続けている。テスト自体は品質が高く
「将来配線されたときの保険」の価値はあるため、即削除ではなく 4-1 の個別精査と
セットで判断すべき。

> **訂正（#44 ラウンドの再実測）**: 57 ファイル / **844 テスト関数 / 8,291 行**
> （テスト関数の 27%・行数の 28%）。母数の増加により比率は 32% から低下している。

### 4-4. ~~虚構の 5 マイクロサービス docker-compose~~（解消済み）

実在しない 5 つの Dockerfile を参照する構成だったが、`0f9a100` で実態
（単一 api サービス）に修正済み。

---

## 5. 過剰に見えて正当なもの（誤検出への注意書き）

将来の監査者が誤って「削除対象」と判定しないための注記:

| 対象 | 一見 | 実際 |
|---|---|---|
| 3D モデル配布機構の不在 | 重大な欠落 | 意図的スコープ（1 節参照）。README が宣言済み |
| `preset_history_*` 系 5 ファイル | preset 残骸の仲間 | `if __name__ == "__main__"` を持つ独立 CLI ツール。削除不可 |
| `billing_service` と `membership_manager` の二重「ティア」 | 概念の重複実装 | ~~別軸（サブスク課金プラン vs 生涯購入額ロイヤルティ）。統合不要~~ **訂正（#44）**: `billing_service.py`（875行, Stripe サブスク）は**リポジトリ全体で import 元がゼロ**であり、並立設計ではなく単なる死コード。`api_server.py` にコメントで言及されるのみ。生存しているティア実装は `membership_manager` だけ |
| クレジット台帳（`_credit_ledger`） | in-memory で脆弱 | append 専用 + `verify_ledger_integrity()` による自己整合性検証つき。アプリ層の設計としては健全（永続化の問題は 3-4 に帰着） |

---

## 6. 推奨着手順

1. **クレジット購入フロー（3-1）** — 有償マーケットプレイスが商売として成立する最低条件
2. **金銭・契約データの削除方針決定（3-3 残り）** — 会計/法務判断。非金銭データのカスケードは実装済み
3. **永続化（3-4）** — 1・2 の成果を再起動で失わないための土台（順序を上げる判断もあり）
4. **死コード削除** — 4-2 の 3 ファイルは即実行可能。残り 57 モジュールは個別精査後に一括判断

~~5. ログイン 2FA 強制~~ — `0b7c24a` で実装済み（セクション 2 参照）
~~6. Prometheus 実スクレイプ対象~~ — `a6aa3f1` で実装済み（セクション 2 参照）
~~7. 非金銭データのアカウント削除カスケード~~ — セクション 2 #11 で実装済み（金銭・契約データの扱いは 3-3 として残存）
~~8. 決済系エンドポイントの冪等性統一~~ — `0ff3cc5` で実装済み（セクション 2 #13）
~~9. legacy 2FA REST エンドポイントの実バグ3件~~ — `47e4137`/`252bd22` で実装済み（セクション 2 #14〜16）
~~10. Web フロントエンドの再構築・接続~~ — `dfff4e3` で実装済み（セクション 2 #17）
