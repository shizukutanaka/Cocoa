# Cocoa プロダクト評価 — 長所・短所・改善案 (Product Assessment)

最終更新: 2026-09-01 / 対象: master (監査エントリ #1〜#89 完了時点)

この文書は、Cocoa アバターマーケットプレイスの現状を「長所」「短所」「改善案」の3部で評価する。
各主張の証跡は `FEATURE_AUDIT.md`(監査 #1〜#89)にあり、番号で参照する。

> **実測された健全性(#89 時点)** — 推測ではなく実行結果:
> - **API 227ルート全数**(GET 108 ＋ 書き込み 119)に実リクエスト → **予期しない 500 ゼロ**。残る 5xx は「DB未構成」「prometheus_client未導入」を正直に伝える 503 のみ(#47 の規約)。再現手順は `scripts/smoke_api.py`。
> - **フロントエンド 32ルート**を Chromium で巡回 → **未捕捉JSエラー 0 / API 5xx 0 / 空白ページ 0**。
> - バックエンド **978 テスト(コア6スイート)・0 失敗**。フロント **69 テスト**・build・lint すべて緑。
> - **重要導線の E2E 11チェック**(公開→検索／UIログイン→リロード維持／**カート購入で両者の残高が動く**／払い戻し→承認→全額回復／通報→コンソール到達)が `scripts/e2e_critical_flows.py` で再現可能(#80)。
> - **特権50ルート**(`/api/admin/*` ＋運用系)に一般ユーザー・未認証で到達を試行 → **2xx ゼロ**。
> - `COCOA_STATE_DIR` 設定時、実プロセス2台の再起動でアカウント・残高・注文・レビュー・検索が**完全一致**。
> - `main/` は製品が実際に import する **33 モジュールと 1対1**(死コードは #59/#60 で全削除、約 46,000 行)。

---

## 1. 長所 (Strengths)

### 1.1 機能の網羅性 — バックエンド
- FastAPI モノリス `main/api_server.py` に **227 ルート**(GET 108 ＋ 書き込み 119)。マーケットプレイスのコア(出品・検索・カート・チェックアウト・レビュー)に加え、以下がすべて実装済み:
  - プロモコード / バンドル / ギフトカード / チップ / 返金申請
  - 紹介プログラム(登録時適用 → 初回購入でボーナス付与、監査 #23)
  - ライセンスキー発行・照合、出品バージョン履歴、価格履歴
  - コミッション(制作依頼)、クリエイター申請、クリエイターダッシュボード / 収益サマリー
  - フォロー / フィード、ウィッシュリスト、保存検索、コレクション、通知(種類別ミュート)
  - VRChat 向け実用ツール(パラメータ予算計算・パフォーマンスランク推定、監査 #39〜#40)
  - 2FA、パスワードリセット(監査 #42)、メール認証、アカウント削除

### 1.2 フロントエンドの配線完了度
- React 18 + TypeScript + TanStack Query v5 で **37 ページ / 32 ルート**。バックエンド完成済み・フロント未接続だった機能を 40 ラウンド超かけて接続済み(監査 #21〜#63)。
- ルート単位のコード分割(React.lazy)、共有クエリキャッシュによる id→名前解決、Idempotency-Key による二重決済防止(チェックアウト / バンドル / チップ / ギフトカード)。
- 型定義 `frontend/src/types/api.ts` はバックエンド `to_dict()` の実出力を読んで正確にミラーしている(推測で書いていない)。

### 1.3 検証済みであること
- バックエンド: unittest **コア6スイート 978 件 / 0 失敗**(フル discover は約2,100件)。うち重要なのはサブプロセス型パッケージ文脈テスト(`TestVRChatToolsPackagedImport`) — 通常スイートでは隠れる import 事故を検出する。
- フロントエンド: Vitest **69 件**(#52〜)＋ 全ラウンドで実 uvicorn + 実ブラウザ(Playwright/Chromium)の E2E を実施し、/api/ への 400 以上の応答を全捕捉して確認。
- **全ルート実測スイープ**(`scripts/smoke_api.py`、#66): 227 ルートに実リクエストを投げ予期しない 5xx がゼロであることを機械的に確認できる。**この手法が #64・#65 の実バグ2件を発見した**(どちらも単体テストがモックで素通りしていた経路)。
- 実バグを **10 件以上**発見・修正済み(下記 1.4)。「動くはず」ではなく「動作を確認した」状態。

### 1.4 修正済みの実バグ(証跡付き)
| 監査# | バグ | 深刻度 |
|---|---|---|
| #35 | 保存型 XSS: プロフィール URL に `javascript:`/`data:` スキームを許容 | 高(セキュリティ) |
| #23 | 紹介プログラムが死蔵: `apply_referral_code()` がどこからも呼ばれていない | 高(機能不全) |
| #39 | VRChat ツールが本番起動形態(`uvicorn main.api_server:app`)で 503 | 高(本番障害) |
| #40 | Quest プラットフォーム指定で 400(存在しない enum 参照) | 中 |
| #36 | 出品譲渡でプロモコードの所有者が旧クリエイターに残る | 中 |
| #36 | プロモコード上限が無効化済みコードも数える(生涯上限化) | 中 |
| #41 | React error #31: performance issues をオブジェクトのまま描画してクラッシュ | 中 |
| #44 | 有料出品が 0 クレジットで引き渡され、**売り手に一切入金されない** | 高(金銭) |
| #49 | 管理操作の実行者が記録されず、自己BAN防止も一度も発火しない | 高(説明責任) |
| #54 | 運用エンドポイント3本が一般ログインユーザーに開放(OWASP API5:2023) | 高(セキュリティ) |
| #64 | 管理者のライセンス失効が**全呼び出しで 500**(唯一の失効手段が到達不能) | 高(機能不全) |
| #65 | DB未構成時に `/api/avatars` が 500 / 空の成功200 / **偽の作成成功**を返す | 中(データ損失の罠) |

### 1.5 プロセス資産
- `FEATURE_AUDIT.md`: 全 **89 エントリ**が「根本原因 → 修正 → 検証方法 → コミットハッシュ」の形式で記録済み。§3 に事業判断保留項目、§4 にデッドコード所見(#59/#60 で解消)。
- 確立済みのラウンド手順(契約読解 → 型ミラー → 実装 → build/lint → E2E → 回帰 → PR → master マージ → ブランチ再スタート)は `HANDOFF_INSTRUCTIONS.md` に文書化。

---

### 1.6 長所の限定 — ソクラテス問答による自己吟味（#87）

長所を数字で主張することには落とし穴がある。この節は**自分の主張を尋問した結果**を残す:

- **「テスト978件が緑」→ だから正常か?** **違う**。#44（有料出品が0クレジットで引き渡され
  売り手に入金されない）は当時の**全単体テストを通過**していた — テストが**レスポンス形状**を
  見て**残高**を見ていなかったため。#81 の並行性テストも、ロックを no-op に置換しても通る
  （GIL がこれら短いクリティカルセクションで切り替わらない）ことを実測済みで、
  **不変条件は守るがロックの存在は守らない**。緑は「調べた範囲で壊れていない」であって
  「正しい」ではない。
- **「227ルート」→ だから機能が豊富か?** **広さであって資産ではない**。
  `scripts/unwired_endpoints.py` の実測で **47本はフロントから一度も呼ばれない**。
  #68 のように「ユーザーがデータを投入できるのに誰も取り出せない」行き止まりも実在した。
  未配線は `wire it / delete it / leave it` を**意識的に選ぶ**対象で、数に数えるものではない。
- **「実測で検証した」→ その観測は正しいか?** #87 では失敗注入 `chmod 500` が
  **root では無効**（権限を無視して書ける）で、危うく「失敗を検出できた」と
  誤って結論するところだった。また `ls \| head -4` の切り詰めを見て
  「バグ発見」と早合点しかけた。**観測手法そのものも尋問の対象**。

## 2. 短所 (Weaknesses)

### 2.1 ~~インメモリ永続化(最大の制約)~~ → **#71/#74 で解消(オプトイン)、残るは複数ワーカー**
~~全ストアがプロセス内メモリのシングルトンで、サーバー再起動で全データが消える。~~
`COCOA_STATE_DIR` を設定すると**15ストア全て**(アカウント・クレジット残高/台帳・出品・カート・注文・
ウィッシュリスト・コレクション・コミッション・ライセンス・ギフトカード・紹介・保存検索・会員・
払い戻し・通知・バンドル)が `state.json` に往復保存され、**再起動を生き延びる**。既定は従来どおり
完全インメモリ。`state_codec` が `to_dict()` の非可逆性(購入済みパラメータの欠落)を回避して
生きたオブジェクトを正確に復元し、検索インデックスは復元後に再構築する。破損スナップショットや
クレジット台帳の不整合では**起動を拒否**(fail-closed)。

**運用可観測性（#87 で追加）**: 保存の成否・時刻・エラー・ストア数を記録し、
`/ready` は保存が失敗し始めると **degraded**、管理コンソールの概要タブは
**保存中／失敗（損失窓つき）／無効**の3態を表示する。以前は失敗してもログ行しか残らず、
運用者はデータを失った再起動で初めて気づく状態だった。

**「約束を取り消すクラッシュ」（#88/#89 で解消）**: 応答が状態を約束する操作
（削除・BAN・BAN解除・取り下げ・復元）は、次のティック前のクラッシュで**元に戻っていた** —
実測で**削除したアカウントが再ログインでき、BANした悪質アカウントも戻った**。
これらは即時保存する。高頻度の書き込みは従来どおりティックを待つ
（購入の消失はユーザーに見えて再実行できるが、消去や処分は取り消されると気づかれない）。

**残る制約**: **単一プロセス限定**。複数ワーカーはアカウントをワーカー間で分断し
スナップショットを互いに上書きするため、#76 で**排他ロックにより起動を拒否**するようにした
(黙って壊れる代わりに)。真の水平スケールには共有ストア(PostgreSQL/Redis)への移行が必要で、
これはインフラ費用を伴う事業判断として保留(§3-4)。

### 2.2 実決済が存在しない
クレジットは内部通貨のみ。Stripe 等による入金、クリエイターへの出金(換金)は未実装。料金体系・手数料率・法務(資金決済法)の判断が必要なため意図的に保留(§3-1, §3-2)。

### 2.3 ~~メール送信がない~~ → **#51 で解消**
`main/email_sender.py`(`ConsoleEmailSender` 既定 / `SMTPEmailSender` は `COCOA_SMTP_*` で構成)を新設し、パスワードリセット・メール認証・再送の3経路に配線済み。応答内トークンは開発用フラグ(`COCOA_EXPOSE_RESET_TOKEN` / `COCOA_EXPOSE_VERIFY_TOKEN`、既定 OFF)にゲート。**SMTP プロバイダの契約選定のみ事業判断として残る**。

### 2.4 ~~管理者 UI がない~~ → **#46 / #49 / #50 / #58 / #62 / #63 で解消**
`/admin` のモデレーションコンソールに7タブ: 概要(プラットフォーム統計)・出品の通報・レビューの通報・払い戻し申請・クリエイター認定申請・ユーザー(名簿検索＋クレジット付与)・停止中ユーザー(解除)。重大度トリアージ、判断理由の必須化、取り下げの復元導線、出品者の違反履歴、通報者への結果通知(#61)まで含む。**未接続の admin エンドポイントはモデレーションキュー・会員ティア調整・監査エクスポート等が残る**(`FEATURE_AUDIT.md` §3-5)。

### 2.5 CI がない(ワークフローは用意済み・導入はオーナー操作)
リポジトリに GitHub Actions 等のチェックが一切ない(status total_count 0)。**git トークンが
`.github/workflows/*` を push できない**ことを実際に試して確認済み(GitHub が
`refusing to allow a GitHub App to create or update workflow ... without \`workflows\` permission`
で拒否)。**判断ではなく権限境界**のため、動作するワークフローを `docs/ci/ci.yml` に同梱し、
1コマンド(`git mv docs/ci/ci.yml .github/workflows/`)で有効化できるようにした。
内容は本プロジェクトの防衛線そのもの(コア4スイート＋フロント build/lint/test)。

### 2.6 ~~フロントエンドの自動テストがない~~ → **#52 で解消**
Vitest + Testing Library で **69テスト**(ロール/ラベル経由のクエリでアクセシビリティの回帰も兼ねる)。
**Playwright E2E も #80 でリポジトリに入った**(`scripts/e2e_critical_flows.py`、重要5導線11チェック)。
バックエンドは **978テスト**。

### 2.7 その他
- i18n なし(日本語ハードコード)。
- モノリス構造: `main/api_server.py` が非常に大きく、APIRouter 分割されていない。
- スナップショットは**ストア単位**でロックを取るため、ストアを跨ぐ操作(チェックアウト＝
  marketplace で課金 → cart で注文作成)が引き裂かれる理論上の窓が存在する。実測では
  積極的な条件下で 31 スナップショット中 0 件。全ストアのロックを同時取得する修正は
  デッドロックの危険を持ち込むため、確率(マイクロ秒の窓 × 30秒周期 × その直後のクラッシュ)に
  見合わないと判断し**記録に留めている**。clean shutdown 時の保存はトラフィックが
  止まっているため常に整合。
- ミューテーションの多くが `useMutation` でなく手書き try/catch(動作は正しいがボイラープレート)。
- ~~デッドコード: リポジトリ全体では到達不能モジュールが多い(§4-1)~~ → **#59/#60 で解消**。`uvicorn main.api_server:app` の import 閉包に基づき到達不能な 65 モジュール+死テスト+`services/` スキャフォールドを削除し、`main/` は製品が実際に import する **33 モジュールと1対1**になった。ルートの旧世代フラット版モジュールも削除済み。

---

## 3. 改善案 (Improvement Proposals) — 優先度順

**P1 = 商用化のブロッカー / P2 = 品質・運用上重要 / P3 = 磨き込み**

| 優先度 | 項目 | 内容 | 判断者 |
|---|---|---|---|
| ~~P1~~ | ~~永続化層~~ **→ #71/#74 で解消(オプトイン)** | `COCOA_STATE_DIR` で15ストア全てが `state.json` に往復保存。残るのは**複数ワーカー**(共有ストアが必要) | 完了(水平スケールのみ保留) |
| P1 | 実決済 | Stripe Checkout でクレジット購入、Stripe Connect で出金 | **事業判断が必要 — 提案のみ** |
| P1 | CI 導入 | workflow は `docs/ci/ci.yml` に**作成済み**。`git mv` で有効化するだけ。トークンが `.github/workflows/*` を push できないことを実測確認 | オーナー(1コマンド) |
| ~~P2~~ | ~~メール送信抽象化~~ **→ #51 で解消** | `email_sender.py`(Console/SMTP)をリセット・認証・再送に配線済み。プロバイダ契約のみ事業判断 | 完了 |
| ~~P2~~ | ~~管理者 UI~~ **→ #46/#49/#50/#58/#62/#63 で解消** | 7タブのモデレーションコンソール。残りの未接続 admin エンドポイントは §3-5 | 完了(一部継続) |
| ~~P2~~ | ~~frontend テスト~~ **→ #52/#80 で解消** | Vitest + Testing Library **69 テスト**（統合主・単体従）＋ Playwright E2E をリポジトリに移植（`scripts/e2e_critical_flows.py`、重要5導線11チェック） | 完了 |
| P3 | i18n | 文言の辞書化(ja → ja/en) | 実装可 |
| P3 | 残小粒機能 | メール認証の再送 UI / Creator ページのフォロワー・フォロー中一覧 / リーダーボード・トレンドタグのウィジェット | 実装可 |
| P3 | リファクタ | 手書きミューテーションの `useMutation` 化、`api_server.py` の APIRouter 分割 | 実装可 |

### 実装禁止の境界(重要)
以下は**事業・法務・金銭の判断を伴うため、ユーザーの明示指示なしに実装してはならない**(`FEATURE_AUDIT.md` §3 に記録済み):
1. Stripe 等によるクレジット購入(実金銭の受領) — §3-1
2. クリエイター出金・換金 — §3-2
3. 金融データの削除ポリシー(アカウント削除時の取引記録の扱い) — §3-3
4. ~~永続化層への移行~~ → **#74 で解消**。残るのは共有ストアへの移行(複数ワーカー対応)で、これはインフラ費用の判断 — §3-4

---

## 4. 次に着手するなら

**事業判断を要さない範囲の主要項目は出尽くしている**(P1〜P2 はすべて完了か、下記の「オーナー判断待ち」に分類済み)。残るのは磨き込みのみ:

1. **残りの未接続 admin エンドポイント(P3)** — 会員ティア調整・監査エクスポート・クオータ設定(§3-5)。
   モデレーションキュー(コミッション紛争)は #68、ロール変更は #73 で配線済み。
   `python3 scripts/unwired_endpoints.py` で現在の未配線一覧が出る
2. **リファクタ(P3)** — 手書きミューテーションの `useMutation` 化、`api_server.py` の APIRouter 分割。**動作を変えない整理**なので、`scripts/smoke_api.py`(227ルート)＋950テストが安全網になる
3. **i18n(P3)** — 文言の辞書化(ja → ja/en)

### オーナーの判断が必要で、これ以上コードでは進められないもの

| 項目 | なぜ止まっているか |
|---|---|
| 水平スケール(§3-4) | 単一プロセスでの永続化は #74 で解消済み。**複数ワーカー**には共有ストア(PostgreSQL/Redis)が必要で、インフラ費用の判断。現状は #76 で**起動を拒否**するため、黙って壊れることはない |
| 実決済(§3-1) | 実金銭の受領。料金体系・手数料率・資金決済法の判断 |
| クリエイター出金(§3-2) | 換金・本人確認・税務 |
| 金融データの削除ポリシー(§3-3) | アカウント削除時に取引記録をどう扱うかは法務判断 |
| CI 導入 | workflow は `docs/ci/ci.yml` に用意済み。トークンが `.github/workflows/*` を push できないことを実測確認したため、**オーナーが `git mv` する1コマンド**だけが残る |

**残るのは、技術ではなく事業・法務・インフラ費用の意思決定を待っている項目だけ**である。
実決済・出金・削除ポリシーは判断そのものが必要で、水平スケールは費用の判断、CI は権限の付与。
コードで前進できる範囲は出尽くしている。

作業手順・環境の落とし穴・検証コマンドはすべて `HANDOFF_INSTRUCTIONS.md` を参照。
